````
/fable-it

The ontology client in brain-api-core reads its LLM API key from env only. trigger-draft.service.ts in the same codebase already solves this correctly: resolve the per-Space backbone key first, fall back to the env var. Bring the ontology client to the same behavior.

/goal Ship the ontology-client key-source fix to done: the client resolves its LLM key from the Space's backbone key, falling back to the existing env var, mirroring trigger-draft.service.ts.

Context (unvalidated — compile ran outside brain-api-core; confirm these paths from its repo root first):
- trigger-draft.service.ts — the pattern to mirror; it already implements per-Space key resolution with env fallback.
- The brain-api-core ontology client module — the file to change (assumed: one module owns the ontology LLM call and its key read — flag if the key is read in multiple places).

DoD:
1. Ontology client resolves the key per-Space with env fallback in the same resolution order as trigger-draft.service.ts — proven by a test exercising both sources.
2. A Space with a backbone key configured uses that key; a Space without one falls back to the env key — proven by the ontology flow/endpoint that invokes the client.
3. Existing test suite stays green.

Out of scope / do not touch: trigger-draft.service.ts itself (reference only), the backbone-key storage/encryption model, LLM clients other than the ontology client.
````

🎯 Target: fable-it — deliverable goal with clear done-ness ("ship to done", mirror an existing pattern), run autonomously with subagent fan-out.

💡 Dropped your teams/lower-models directive from the prompt on purpose: fable-it owns model tiering canonically (and your global CLAUDE.md tiering default already binds it — coordinator on the big model, cheap models for mechanical slices), so routing to fable-it delivers exactly that without restating it. Both file pointers are cross-tree (this session is sandboxed to the prompt-it repo) and are marked unvalidated accordingly.

Run from the brain-api-core repo root.
