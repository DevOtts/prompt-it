```
/fable-it

Context: a review of brain-admin-ui confirmed three localization failures. All three are
instances of ONE class — locale-dependent presentation hardcoded to pt-BR — not three
independent bugs. A previous fix inlined ternaries on `tenant.language` at each call site
and was reverted for being unmaintainable.

/goal Introduce a single locale/formatting layer in brain-admin-ui that all tenant-facing
presentation (UI strings, dates, currency) derives from, and fix the three confirmed
instances through it — so the next hardcoded string is a one-line dictionary entry, not
another ternary.

Confirmed findings (verified failing — do not re-verify from scratch, go straight to fix):
1. Header title hardcoded "Painel" — English tenants see Portuguese.
2. Dates hardcoded DD/MM/YYYY in the tasks table.
3. Currency symbol hardcoded "R$" in the billing widget.

Mechanism constraint: one shared module owns locale resolution and formatting (e.g. a
translation dictionary for strings + `Intl.DateTimeFormat` / `Intl.NumberFormat` for
dates/currency, keyed off the tenant's locale). The three surfaces consume it; the
specific library/shape is your call. (assumed: `tenant.language` is the only locale
signal available — derive locale from it rather than adding tenant fields; flag if a
richer locale/currency field exists.) (assumed: billing amounts and currency selection
are already correct per tenant and only the display symbol is wrong — flag if currency
choice itself is broken, that's a different fix.)

Failed attempt — do NOT re-propose: inline `tenant.language` ternaries at each call site.
Reverted once already as unmaintainable.

DoD sketch:
1. Header title renders in the tenant's language — dashboard header shows English text
   under an English tenant, Portuguese under a Portuguese tenant.
2. Tasks-table dates render in the tenant's locale format — locale-appropriate order/
   separators under both tenants.
3. Billing widget currency formats per tenant locale — correct symbol/placement under
   both tenants.
4. No per-call-site language conditionals remain in the three touched components — all
   locale logic lives in the shared layer (grep of those components proves it).
5. The three touched components contain no OTHER hardcoded pt-BR strings/formats of the
   same class — sweep them and route anything found through the same layer.
   (assumed: sweep limited to the three touched components, not app-wide — flag if you
   want the whole admin UI swept.)

Out of scope / do not touch: backend APIs, tenant data model changes, translating admin
UI surfaces beyond the three touched components, i18n of non-tenant-facing internals.
```

🎯 Target: fable-it — fix + verify against confirmed review findings; three surfaces sharing one mechanism-level fix is a small autonomous delivery, not a plan-it-sized scope.

💡 Reframed the three point defects as one class (missing locale layer) and pinned the reverted ternary approach as a forbidden retry — the two things the next session would otherwise get wrong again. `brain-admin-ui` lives outside this working tree, so no pointers could be validated (unvalidated — target session must confirm paths, and should read the review report in `.review-it/` if one exists).

Run from the brain-admin-ui repo root.

Optional refinements (answer and I'll tighten):
1. Does the tenant model carry only `language`, or is there a proper `locale`/`currency` field the layer should key off instead?
2. Is there an existing i18n dependency already in brain-admin-ui's package.json (react-i18next? next-intl?), or is the session free to choose?
3. Sweep scope: just the three touched components (current assumption) or the whole admin UI?
