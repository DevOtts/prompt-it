```
/fable-it

Grounding: In brain-api-core, the ontology OpenRouter client resolves its API key
from the global pod env var only, ignoring the Space's own backbone connection.
trigger-draft.service.ts already implements the correct resolution chain — this is
a mirror job, not new design.

/goal Ontology LLM calls authenticate with the per-Space "Direct (OpenRouter)"
backbone connection key when the Space has one, falling back to env vars otherwise —
identical resolution to trigger-draft.

Context package:
- brain-api-core repo root — the working tree for this run (unvalidated — target
  session must confirm this path first)
- trigger-draft.service.ts — the pattern to mirror: per-Space "Direct (OpenRouter)"
  backbone connection key → OPENROUTER_API_KEY → OPENAI_API_KEY (unvalidated —
  confirm file and read its resolution logic before writing code)
- the ontology OpenRouter client — the file to change; locate it by grepping the
  ontology module for the OpenRouter env-var read (assumed: a single client/service
  owns this read — flag if the key is read in multiple ontology call sites)

DoD sketch:
1. Ontology client resolves the key in order: Space's "Direct (OpenRouter)"
   connection → OPENROUTER_API_KEY → OPENAI_API_KEY.
2. A Space WITH a Direct (OpenRouter) connection: its ontology requests carry the
   Space's key (outbound request auth / logs prove it).
3. A Space WITHOUT one: ontology calls still work on the env-var fallback —
   existing behavior unchanged.
4. trigger-draft.service.ts behavior is untouched (reusing/extracting its resolver
   is fine if behavior-preserving).
5. Shipped: merged and deployed; the ontology flow verified working in the
   deployed environment.

Out of scope / do not touch: other backbone connection types (only Direct
OpenRouter), key resolution in non-ontology services beyond any shared helper
extraction, and any change to which models/endpoints the ontology client calls.
```
🎯 Target: fable-it — "build and ship" is a full goal-to-DoD delivery (implement + deploy + verify), not a single in-session loop.
💡 All three pointers are cross-tree (brain-api-core; this session is sandboxed to the prompt-it repo), so they're kept but marked unvalidated — the target session must confirm them before coding; I also surfaced the assumption that one client owns the env-var read.

Run the prompt from the brain-api-core repo root, not from prompt-it.
