```
Organize the top level of ~/Downloads by moving files into subfolders by type. Move only — never delete a file, never overwrite one.

Categories (create a subfolder only when it has matches):
- Images/     → png, jpg, jpeg, gif, webp, heic, svg, tiff, bmp
- PDFs/       → pdf
- Archives/   → zip, tar, gz, tgz, rar, 7z, xz, bz2
- Installers/ → dmg, pkg, msi, exe, iso

Rules:
1. Touch only files at the top level of ~/Downloads — do not recurse into or move existing subfolders.
2. Files matching no category stay where they are.
3. On a name collision in the destination, append a numeric suffix (report-1.pdf) — never overwrite.
4. Skip in-progress downloads (.download, .crdownload, .part) and anything you're unsure how to classify — note it instead of guessing (assumed: unmatched files stay at top level rather than going into an Other/ folder — flag if wrong).
5. Write nothing outside ~/Downloads. If ~/Downloads doesn't exist or is empty, stop and say so.

When done, print a summary table (category → files moved) plus the list of skipped/unmatched files, then verify by running `ls` on each created subfolder and showing the output.
```

🎯 Target: bare Claude Code session — user explicitly wants plain, plugin-free paste; no harness skills assumed.

💡 Added the safety rails a bare agentic run doesn't get for free — collision suffixing instead of overwrite, no-recurse scope lock, skip-partial-downloads, and a run-and-show `ls` verification; `~/Downloads` itself couldn't be validated from this sandboxed session (cross-tree), but rule 5 makes the target session confirm it before acting.

Setup: launch the session with access to the folder, e.g. `cd ~/Downloads && claude` or `claude --add-dir ~/Downloads` — otherwise every `mv` will hit permission prompts.

1. Should unmatched files go into an `Other/` subfolder instead of staying at the top level? (Prompt currently assumes stay-put.)
2. Do you want `.app` bundles and disk-image leftovers treated as Installers too, or left alone? (Prompt currently only moves the listed extensions.)
