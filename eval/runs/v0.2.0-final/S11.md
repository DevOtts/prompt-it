```
/plan-it

/goal Add a CONTRIBUTING.md to this repo covering how to add a new target profile to the per-target ledger `plugins/prompt-it/skills/prompt-it/references/targets.md` (the file README shorthands as `references/targets.md` — the repo has no root-level `references/` dir).

Grounding: contributors need a documented path for extending prompt-it's per-target responsibility map (the owns/parses/cautions ledger) without reverse-engineering the existing profile structure.

DoD:
1. CONTRIBUTING.md exists at repo root with a "how to add a new target profile" section — verification target: its steps mirror the actual structure of an existing profile in targets.md (route name, "Never emit", "Must ADD", cautions), not invented fields.
2. CONTRIBUTING.md is linked from README.md — verification target: a resolving link in README (natural fit: the top nav row or the Architecture section that already cites targets.md at README:97/112).

Out of scope / do not touch: no CI changes; no edits to targets.md's actual profiles or to SKILL.md.
```

🎯 Target: **plan-it** (locked — you named it) — but this is a single documentation file with a fully-specified DoD; **fable-it** or a bare session would ship it in one pass without plan-it's discovery→spec→agile machinery. Your call; route kept.

💡 Fixed a dead pointer: `references/targets.md` doesn't exist at repo root — the real ledger is `plugins/prompt-it/skills/prompt-it/references/targets.md` (README uses `references/targets.md` as shorthand). Left as flagged so the doc references the true path.
