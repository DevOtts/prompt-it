```
/fable-it

Continuation of the trace-naming work (Langfuse project `praxya`). Review verdict: the chat path WORKS — traces from chat arrive named "chat", verified in Langfuse. Do not re-fix it.

/goal Every LLM call in this system arrives in Langfuse with a real origin name — zero traces left as bare "llm.generate".

Remaining gaps (likely two instances of one class: naming was wired at the chat entry point, not at the shared call layer):
1. brain-apps/pulse — 3 call sites still emit bare "llm.generate" (unvalidated — target session must confirm by grepping brain-apps/pulse for the LLM client / "llm.generate" usage first).
2. Automation-triggered calls — also arrive unnamed (assumed: a separate entry path from both chat and pulse — flag if wrong).

Prefer the mechanism-level fix: if these call sites share one LLM client/wrapper, make the origin name required (or caller-derived) at that layer so an unnamed call can't ship — patch per-site only if no shared layer exists. Why: the chat-only wiring is exactly how these two gaps appeared; a third entry point would repeat it.

DoD sketch:
1. A fresh run of each pulse call site shows a distinct origin name in Langfuse project `praxya` — no new "llm.generate" traces from pulse.
2. A fresh automation-triggered call shows its origin name in `praxya`.
3. Chat traces still arrive named "chat" in `praxya` (regression check).

Out of scope / do not touch: the chat naming path (verified working); trace payloads, sampling, or any other Langfuse configuration — origin naming only.
```
🎯 Target: fable-it — two gaps across separate entry paths with external (Langfuse) verification; more than one tight loop, so iterate doesn't fit.
💡 Reframed the two FAILs as one class (naming enforced at the shared LLM client layer, not per entry point) and added a chat-regression DoD item; all brain-apps pointers are outside this repo's tree, so they're marked unvalidated rather than verified.

Run the prompt from the brain-apps repo root — this session is sandboxed to the prompt-it repo and could not confirm its path or the 3 call sites.
