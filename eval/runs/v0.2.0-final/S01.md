```
/plan-it

We run a multi-tenant "brain" platform. Third parties (HubSpot, Airtable, etc.) now ship official MCP servers, but tenants have no plug-and-play way to install them — every integration today is bespoke. The integrations settings area already has sections for backfill and webhooks, which is the pattern to extend.

/goal: Tenants can install, configure, and remove third-party MCP servers from a new MCP section in integrations settings, and both brain-agent and brain-apps can discover and use each tenant's installed MCPs at runtime.

Context package (all unvalidated — this was compiled outside the brain repo; confirm each path before pre-grounding):
- Integrations settings module, backfill + webhooks sections (assumed: lives in brain-apps — flag if wrong) — the UI/UX and persistence pattern the MCP section should mirror.
- brain-agent — the runtime that must discover a tenant's installed MCPs and expose their tools to the agent loop; find its existing tool/integration registry as the attachment point.
- brain-apps — needs read visibility of installed MCPs (assumed: for display/selection, not execution — flag if wrong).
- Tenant-scoped credential storage used by existing integrations — MCP servers need per-tenant auth (API keys / OAuth); reuse it, don't invent parallel storage.

Success sketch (outcome-level; lock the real DoD during planning):
1. A tenant can add an MCP server (assumed: remote HTTP/SSE servers like HubSpot's and Airtable's hosted MCPs, not tenant-uploaded stdio processes — flag if wrong) via the integrations settings UI, verified by the new section listing it as connected.
2. brain-agent sessions for that tenant can list and invoke the installed MCP's tools, verified by an agent conversation using a HubSpot or Airtable tool.
3. Installed MCPs are tenant-isolated — tenant A's install and credentials are invisible to tenant B, verified at the persistence layer and the agent runtime.
4. Removing an MCP revokes it everywhere (settings list, agent tool discovery) without residue.

Out of scope / do not touch: existing backfill and webhook sections' behavior; building our own MCP server marketplace or hosting tenant-authored MCP servers; migrating existing bespoke integrations to MCP. Credentials are stored via the existing secret mechanism — never in plain config.
```
🎯 Target: plan-it — multi-surface feature (settings UI + persistence + two runtime consumers + tenant auth/isolation) with unresolved scope; needs discovery and a delivery package, not a direct build.
💡 This session is sandboxed to the prompt-it repo, so every brain-repo pointer is marked unvalidated rather than dropped — the plan-it session must confirm paths first; I also flagged the two assumptions doing the most work (remote-only MCP transport; brain-apps needs read-only visibility).

Run this from the brain platform's repo root (or the monorepo containing brain-agent + brain-apps).

Optional refinements — answer any and I'll tighten the prompt:
1. Transport scope: remote hosted MCP servers only (HTTP/SSE + OAuth/API key), or must tenants also be able to point at arbitrary/self-hosted MCP endpoints?
2. Does brain-agent already have a pluggable tool-registry layer that MCP tools would slot into, or is dynamic tool discovery itself new work?
3. Are integrations settings UI and its backend in the same repo (brain-apps), or is there a separate API/service that owns integration state?
