```
/iterate

/goal Make `pnpm build` pass. It currently fails with:
`src/eval/runner.ts(42,18): error TS2345: Argument of type 'string | undefined' is not assignable to parameter of type 'string'.`

Fix the type error at its source — resolve why the value can be `undefined` at that call site (guard, narrow, or fix the upstream type), not by suppressing it (`!`, `as string`, `@ts-ignore` are out unless the invariant is provably guaranteed and documented in one line).
(assumed: the repo containing `src/eval/runner.ts` with a pnpm build — this is NOT the prompt-it skill repo, which has no package.json; run from that project's root — flag if wrong)

DoD:
1. `pnpm build` exits 0 — paste the clean exit as evidence.
2. No new type suppressions introduced (`grep` the diff for `ts-ignore`/`as string`/`!.` additions).

Out of scope / do not touch: anything beyond what this one error requires — no refactors of runner.ts, no dependency or tsconfig changes unless the error is provably a config defect.
```

🎯 Target: /iterate — single fix-test-verify loop against one undisputed failing command; no planning or autonomy machinery needed.

💡 The pointer `src/eval/runner.ts` does not exist in the current repo (prompt-it — no package.json/pnpm here), so it's kept as a cross-tree pointer the target session must confirm; the prompt adds the no-suppression constraint and evidence-bearing DoD your one-liner left implicit.

Run this from the root of the repo that actually owns `src/eval/runner.ts`.
