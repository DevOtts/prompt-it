```
/goal Document the eval suite under eval/ in this repo's architecture docs so a contributor can understand what it is, how to run it, and where results live.

Create `docs/architecture/overview.md` (assumed: neither the file nor a `docs/` dir exists in this repo — you are CREATING it, not updating; flag if you meant a different doc, e.g. README.md).

Context — read these first (all validated on disk):
- eval/sample-prompts.md — the eval input set
- eval/expected-prompts.md — expected outputs / grading reference
- eval/scripts/run-eval.sh — the runner
- eval/fixtures/notes.md — fixture notes
- eval/runs/ — versioned run outputs (v0.1.0, v0.2.0, v0.2.0-final)
- CHANGELOG.md — v0.2.0 claims "20/20 on binding eval suite"; the doc must stay consistent with it

DoD:
1. docs/architecture/overview.md exists, covering: purpose of the eval suite, layout of eval/ (each file/dir above), how a run executes (run-eval.sh), and how outputs are versioned under eval/runs/.
2. Every path named in the doc exists on disk — verify with ls before finishing.
3. README.md gains a one-line link to the new doc so it's discoverable.

If a file's purpose is unclear, read it and infer; if still ambiguous, record it in the doc as an explicit TODO instead of guessing.

Output: the new doc + the README link; show the final doc content when done.

Out of scope / do not touch: eval/ contents themselves, SKILL.md, plugin code under plugins/, CHANGELOG.md. No new tooling or scripts.
```

🎯 Target: bare session — a single scoped doc-writing task; no harness orchestration (plan/QA/overnight) is warranted.

💡 Your pointer was dead: `docs/architecture/overview.md` doesn't exist and the repo has no `docs/` directory at all — reframed as *create* (flagged inline), inventoried the actual eval/ contents as validated pointers, and added a README link so the new doc isn't orphaned.

Run from the prompt-it repo root (`~/Workspace/Devotts/prompt-it`).
