```
/fable-it

/goal: Guard this repo against release-version drift — a script that fails loudly the moment the four version-bearing files disagree, proven by a demonstrated catch.

Version sources (all validated on disk, all currently 0.2.0):
- .claude-plugin/marketplace.json — plugin entry "version" field
- plugins/prompt-it/.claude-plugin/plugin.json — "version" field
- SKILL.md — frontmatter `version:` line (assumed: the script must ALSO check the second copy at plugins/prompt-it/skills/prompt-it/SKILL.md, which carries its own version — flag if only the root copy matters)
- CHANGELOG.md — the release version is the first `## X.Y.Z` heading

DoD:
1. scripts/check-version.sh exists, is executable, and exits 0 on the current tree.
2. It exits non-zero when any one source's 0.x.y version differs from the others, printing which file(s) disagree.
3. The run report shows evidence of a real catch: an invocation against a mismatched state returning non-zero, with the tree restored afterward (git status clean except the new script).

Out of scope / do not touch: no version bumps, no CI/git-hook wiring, no edits to eval/ or the four version files beyond any temporary mismatch you fully revert.
```
🎯 Target: fable-it — deliverable goal with done-ness stated ("prove it catches a mismatch"), run unattended to completion while the user is out.

💡 Validated all four pointers: "plugin.json" resolves to `plugins/prompt-it/.claude-plugin/plugin.json`, "marketplace.json" to `.claude-plugin/marketplace.json`, and the repo has TWO SKILL.md copies both at 0.2.0 — the prompt assumes the script checks both (flagged inline).

Run from the prompt-it repo root.
