```
Write data/customers.csv is corrupted by duplicates — same email in different casings counts as one customer. Create `scripts/dedupe_customers.py` (Python, stdlib `csv` only — no pip installs) that:

1. Reads `data/customers.csv`, matches the email column by case-insensitive header name (assumed: a column literally named "email" — flag if it's different).
2. Treats two rows as duplicates when their emails match after lowercasing + trimming whitespace, regardless of other column differences.
3. Keeps the FIRST occurrence of each email (assumed — flag if you want last-wins or a merge rule).
4. Writes the result to `data/customers.deduped.csv`, preserving the header and original column order — do NOT overwrite `data/customers.csv` (assumed: original preserved — flag if you want in-place).

If the file is missing, empty, or has no email-like column: stop and print the actual header row instead of guessing a column.

When done, run the script and show its report: total rows read, rows kept, rows removed, output path. Spot-check by grepping one duplicated email in the output — it must appear exactly once.

Do not create or modify anything outside `scripts/` and `data/customers.deduped.csv`.
```
🎯 Target: bare session — scratch repo with no plugins, so no harness route exists; single-deliverable script task.
💡 Pinned the three decisions the ask left open (first-wins keep rule, write-to-new-file instead of in-place, stop-don't-guess if the email column isn't found) as flagged assumptions, and added the run-and-show + spot-check contract so "done" is proven, not claimed. `data/customers.csv` is in your scratch repo, not this one — unvalidated here; the script's missing-file guard covers that.

Run this from the scratch repo root.

Optional refinements (answer and I'll tighten):
1. Keep rule — first occurrence or last occurrence wins?
2. Output — new file `customers.deduped.csv`, or dedupe `customers.csv` in place?
3. Language — Python stdlib OK, or do you want Node/bash instead?
