```
/fable-it

Continuation of the observability epic — post-review iteration (review findings below; fix the two gaps, keep what's verified).

Run from the praxya product repo (assumed — this prompt was compiled outside that tree; confirm the repo root before starting).

VERIFIED WORKING — do not rebuild or touch:
- Langfuse deployed globally, per-tenant projects live, chat traces arriving. Verified in the Langfuse UI, project `praxya`. Treat this deployment and the per-tenant provisioning as frozen.

/goal Close the two review gaps so every LLM interaction in the product is observable end-to-end: user feedback capturable on every LLM surface, and multi-step generations traced per step so cost is attributable.

GAP 1 — feedback UI coupled to the chat page (class, not point defect):
The thumbs component only renders on the chat page, but Intel and Design pages also have LLM interactions. Fix at the mechanism level: the feedback component should attach wherever LLM output renders (assumed: a shared component exists that chat already uses — locate it and generalize its mounting, rather than copy-pasting per page; flag if no shared component exists).

GAP 2 — multi-step intel generation traces flat (class, not point defect):
Intel generation shows as one flat `llm.generate` trace, so cost per step is invisible. Fix at the instrumentation layer: multi-step pipelines should emit one parent trace with a child span per step (Langfuse supports nested spans/generations natively). Apply the mechanism to any multi-step generation, not just intel — intel is the instance the review caught.

DoD sketch:
1. Intel and Design pages each render the feedback thumbs next to LLM output, and submitting feedback there lands as a score/feedback record in Langfuse project `praxya`.
2. Running one intel generation produces a Langfuse trace in `praxya` with one child span per pipeline step, each showing its own token/cost figures — no flat single-span `llm.generate`.
3. Regression: chat-page traces and chat-page feedback still arrive in Langfuse unchanged.

Out of scope / do not touch: the Langfuse deployment itself, per-tenant project provisioning, and the chat tracing path (all verified) — no new observability vendors, no schema changes to existing traces beyond adding nesting.
```

🎯 Target: fable-it — two undisputed, evidence-backed gaps needing fix + verify across frontend and instrumentation; too multi-surface for a single /iterate loop.

💡 Reframed both FAILs as class-level fixes (shared feedback component, pipeline-level span instrumentation) instead of per-page/per-feature patches; the praxya repo lives outside this tree, so its path and the shared-component assumption are flagged inline for the target session to confirm — no review-session ledger card was found here to back-reference.

Run from the praxya product repo root, not from this prompt-it repo.
