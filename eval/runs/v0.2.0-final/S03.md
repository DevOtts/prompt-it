```
/review-it

The team claims the feedback thumbs up/down component is finished and
deployed to the test environment. Before the client is told, verify the
claim is actually true — treat the team's word as the claim under test,
not as evidence.

Claim under test: the completion claims in the last session's report
(assumed: the most recent report/card in the product repo's
`.fable-it-reports/` or `.agents/history/` — unvalidated, no report in
the current tree mentions this component; locate and confirm that report
first — flag if wrong). Verify every item that report marks done, as
observed live on the deployed test environment (assumed: the test-env
URL named in that report — flag if wrong), not just in the codebase.

Out of scope / do not touch: no fixes, no code changes, no redeploys —
verdict + evidence per claimed item only. Production is off-limits.
```
🎯 Target: review-it — "is it actually done before I tell the client" is a verification-of-claim ask, not a build or fix.
💡 Pointer validation failed in this tree: no session report or ledger card in `prompt-it` mentions a thumbs up/down component (the local `.fable-it-reports/report*.md` are about this repo's own eval work) — so the report pointer is marked unvalidated and the review is fenced to verdict-only, with the report itself as the contract instead of a fresh DoD.

Run this from the repo of the product that owns the component — the claim's session report is not in the current (prompt-it) working tree.

Optional refinements — answer and I'll tighten the prompt:
1. Which repo owns the component (so I can pin the exact report path instead of the unvalidated assumption)?
2. Is "the last session's report" a `.fable-it-reports/report.md`-style run report or a session-ledger card in `.agents/history/`?
3. Do you have the test-env URL, or should the review take it from the report?
