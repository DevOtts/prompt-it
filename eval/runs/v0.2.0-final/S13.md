```
/iterate

There's a bug in the Engine workspace's auth: sessions expire well before their configured lifetime.

/goal: Find and fix the ROOT CAUSE of premature session expiry so a logged-in session stays authenticated for its full intended lifetime — not a band-aid that just raises the TTL.

Context:
- Symptom: sessions "die way too early" — user gets logged out / auth fails long before the session should end.
- (assumed: "Engine workspace" is a separate repo — open it and confirm the auth/session module path before touching anything — flag if wrong)
- (assumed: the bug is sessions dying BEFORE their configured TTL, not that the configured TTL is intentionally short and should be raised — flag if the intended fix is just bumping the lifetime value)
- Likely suspects to check before editing: token/session TTL config, refresh-token or sliding-expiration logic, and clock/timezone handling on expiry — confirm which one actually fires.

Done when:
1. Root cause named — why sessions expire early, evidenced (the specific config/logic that cuts the session short), not symptom-patched.
2. Fix applied and demonstrated: a fresh login stays authenticated through the full expected session window — verify by logging in, staying past the point where sessions previously died, and confirming a protected endpoint still returns authenticated (not 401).
3. Existing auth tests pass; add one regression test asserting a session survives to its configured expiry.

Out of scope / do not touch: unrelated auth flows (login UI, SSO/provider config, password reset) unless one of them is the proven root cause.
```

🎯 Target: `/iterate` — single diagnose→fix→verify loop on one bug with derivable done-ness (sessions survive their intended lifetime); root cause is unknown, so it needs the iterate diagnosis loop, not a blind edit.
💡 Pinned the fix to the ROOT CAUSE and flagged two head-context unknowns as inline assumptions — the workspace path (can't validate it from here; it's a different tree) and whether "too early" means a real bug vs. a too-short TTL, which changes the whole fix.

Answer any of these and I'll tighten the prompt:
1. What's the gap — intended session lifetime vs how fast they actually die (e.g. "should last 24h, dies in ~15min")?
2. Which auth mechanism: JWT access/refresh tokens, a server-side session cookie, or an OAuth-provider session?
3. Where's the Engine workspace on disk (path or repo name)?
