```
/iterate

Bug: authenticated sessions expire far sooner than intended (assumed: user login
sessions in the Engine workspace app — flag if you mean engine/job sessions).

/goal Users stay logged in for the full intended session lifetime — find the root
cause of the premature expiry and fix it.

Context:
- Repo: the Engine workspace (unvalidated — confirm this path first; run from its
  root, not from prompt-it).
- Start diagnosis at whatever sets session lifetime: token/cookie TTL config,
  refresh flow, and server-side session store eviction are the usual suspects.

DoD sketch:
1. Root cause identified and stated — which component cuts the session short,
   and why.
2. A session survives to its configured lifetime (assumed: the TTL already set in
   the auth config is the intended value — flag if the config itself is wrong),
   proven by a login that remains valid past the previous early-death point.
3. A regression test covers the early-expiry case; existing auth tests stay green.

Out of scope / do not touch: auth provider swap, login/signup UI, any workspace
outside Engine.
```
🎯 Target: /iterate — a single diagnose → fix → test → verify loop on one bug; no unresolved scope that would need plan-it, no overnight multi-epic run that would need fable-it.
💡 Couldn't validate the Engine workspace path (outside this session's allowed tree — session is sandboxed to the prompt-it repo), so it's marked unvalidated for the target session to confirm; the main ambiguity — what "sessions" and what the correct lifetime is — is pinned as inline flagged assumptions instead of blocking.

Setup: paste into a session opened at the Engine workspace repo root.

Optional refinements (answer and I'll tighten):
1. "Sessions" = user login sessions, or Engine's own job/worker sessions?
2. What's the intended lifetime — the configured TTL is correct and being violated, or is the TTL itself set wrong (e.g. minutes when you want days)?
3. Exact path of the Engine workspace repo, so the pointer ships validated?
