```
/iterate

The CDP e2e suite is red: the tasks-page dropdown test throws "ResizeObserver loop limit exceeded" and the dropdown element detaches mid-click. Run this from the repo that owns the tasks page and its e2e suite (assumed: a sibling app repo, not prompt-it — cd there first; flag if wrong).

/goal Make the e2e suite pass against the real tasks page, with the dropdown click genuinely exercised — skipping, quarantining, or mocking the dropdown does not count as done.

Context — two distinct symptoms; treat them separately until proven linked:
- "ResizeObserver loop limit exceeded" is often browser-benign noise — determine whether the suite fails because a pageerror/console listener escalates it, or because the app genuinely loops.
- The mid-click detach points at a re-render race in the dropdown during interaction — likely a real app or selector-timing issue independent of the ResizeObserver error.
- (assumed: run the suite via the same command CI uses — flag if another entry point is canonical)

DoD sketch:
1. The tasks-page dropdown e2e passes on repeated runs (flake ruled out), with the click landing on the live dropdown element.
2. The full e2e suite is green via the project's standard e2e command.
3. The final report states the root cause of each symptom (app bug vs. test-harness error handling) with the evidence that proves it.

Out of scope / do not touch: no `.skip`/`.only`/quarantine on the failing test; no blanket swallowing of page errors (a genuine app-side error must still fail the suite); no changes to unrelated suites or app features beyond what the dropdown fix requires.
```
🎯 Target: iterate — single diagnose → fix → test → verify loop against an undisputed target (suite green), no planning or multi-epic scope.
💡 Split your one symptom into two separate failure hypotheses (harness escalating a benign ResizeObserver error vs. a real detach race) and fenced off the skip/swallow escape hatches; the tasks-page repo isn't in this working tree, so its path and e2e command are flagged assumptions, not validated pointers.

Setup: paste into a session opened in the app repo, not prompt-it.

Optional refinements — answer and I'll tighten:
1. Which repo/path owns the tasks page? (my sandbox only sees `prompt-it`, so I couldn't confirm a sibling)
2. Which CDP setup should the prompt pin: your per-worktree `parallel-lifecycle` ephemeral Chrome, or the real logged-in Chrome at `:9222` (`chrome-cdp-control`)?
