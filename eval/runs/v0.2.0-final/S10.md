```
Fix the typo "recieve" → "receive" in eval/fixtures/notes.md (line 2, the only occurrence — verified). Touch only that word in that file; change nothing else. If the typo is missing or appears more than once when you look, stop and report instead of editing. When done, show the corrected line via `grep -n "receive" eval/fixtures/notes.md` as proof.
```
🎯 Target: bare session — single mechanical edit; no harness skill earns its overhead here.
💡 Validated the pointer: `eval/fixtures/notes.md` exists and "recieve" appears exactly once, at line 2 — the prompt pins that so the session verifies rather than searches.

Run from the prompt-it repo root.
