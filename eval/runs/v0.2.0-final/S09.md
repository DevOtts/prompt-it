```
/fable-it

Continuation of the Airtable write feature after a failed review. What stands: the
task-write path and the integration-enable flow are implemented, and the CDP e2e suite
executes end to end. What does NOT stand: any green result from that suite — it reported
green while the feature was broken for a human. Treat each finding below as a class of
defect, not a point fix.

Finding 1 — false-green e2e (oracle blindness): the CDP suite passes while the tasks
dropdown throws UI errors when a human runs it. The suite's oracle cannot see this class
of UI error. Fix the dropdown, and make the suite one that goes red on today's broken
build — a green that can't fail is not evidence.

Finding 2 — unverified system of record: saving a task was never confirmed to actually
land in Airtable; verification stopped at the app's own UI, and the integration had to be
enabled first for the save to work at all. The proof of this feature is the record
appearing in the Airtable base itself.

/goal A human can save a task through the dropdown and the record demonstrably lands in
Airtable, with an e2e suite whose green is trustworthy.

DoD sketch:
1. Tasks dropdown opens and saves without UI errors in a human-driven browser session
   (target: the tasks page — zero UI/console errors on open + save).
2. A saved task exists as a record in the Airtable base/table (target: the Airtable
   table itself, not the app UI).
3. The e2e suite is red against the current broken build and green only after the fixes
   (target: suite output on both builds — this is the proof the false green is dead).
4. The integration-enable precondition is inside the verified flow, starting from
   integration disabled (target: end-to-end save succeeding after enable).

Failed attempts — do not re-propose: re-running the CDP suite with longer waits was
already tried and produced the same false green. Timing is not the cause; do not reach
for waits/retries or looser assertions.

Out of scope / do not touch: other integrations, unrelated e2e specs, visual redesign of
the dropdown.

(assumed: this runs from the Airtable feature's repo root — this prompt was compiled
from a different repo, so no file pointers could be validated; first grounding step is
locating the tasks dropdown component, the Airtable write module, and the CDP spec —
flag if wrong)
```
🎯 Target: fable-it — both findings need fixes *plus* re-verification (false green + unchecked system of record), which is fix-and-verify territory, not a pure re-verify (review-it) or a single tight loop (iterate).
💡 Converted both findings from point defects to their classes (oracle blindness; verification stopping short of the system of record), added DoD item 3 so the suite must fail on the known-broken build — the direct antidote to the false green — and captured the longer-waits attempt as a banned retry. No pointers could be validated: the Airtable feature does not live in this repo (`prompt-it` skill repo), so grounding is delegated to the target session's first step.

Setup: paste into a fresh session opened at the feature repo's root, with the Airtable integration credentials available as env vars (`[AIRTABLE_API_KEY]`-style, not inline).

Optional refinement: which repo hosts the Airtable feature? My lookup confirmed it is not this tree — give me the path and I'll validate the component/spec pointers and tighten the grounding step into concrete `@refs`.
