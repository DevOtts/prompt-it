```
/fable-it

Grounding: this ask is already planned — docs/implementation/2-feedback-observability/ (PRD v1.0, G3-approved 2026-07-06) lists this exact request as gaps W1/W2/W3, and epics E3 (design-chat widget coverage) + E4 (review modal & thumbs-down reason form) carry its binding test contracts. Parts may already be built: FeedbackWidget.tsx, RegenerateFeedbackModal.tsx, FeedbackReasonForm.tsx exist on disk, and the later run 0-done/3-sow-feedback-ux (G2-locked 2026-07-16) KILLED popup-on-complete on the SOW editor in favor of an inline feedback box + regenerate-intercept modal.

/goal Close feedback gaps W1–W3 — thumbs on every LLM chat/generation surface (Intel, Design chat, all regeneration flows), an active post-generation review prompt, and a structured "what went wrong" reason on thumbs-down — by finishing/extending the existing feedback-obs package, not rebuilding it.

Context package (read in this order):
- docs/implementation/2-feedback-observability/prds/PRD-master.md — governing PRD; W1/W2/W3 map 1:1 to this ask
- docs/implementation/2-feedback-observability/epics/epics-feedback-obs.md — E3 + E4 are the target epics with binding test contracts; E3 depends on E1 (check E1's merge status, don't rebuild it)
- docs/implementation/0-done/3-sow-feedback-ux/PRD-sow-feedback-ux.md — LATER locked decisions: blocking popup-on-complete was killed (it trapped users); inline box + regenerate-intercept modal is the approved pattern new surfaces must follow
- docs/implementation/2-feedback-observability/research/11-chat-surfaces-and-ids.md — authoritative surface inventory; "every page" means this list, not a fresh audit
- frontend/src/components/FeedbackWidget.tsx, RegenerateFeedbackModal.tsx, FeedbackReasonForm.tsx — existing primitives to extend, never duplicate
- frontend/src/pages/Intel.tsx, frontend/src/pages/DesignChat.tsx — the named uncovered surfaces

DoD:
1. Build-state reconciled first: per E3/E4 checklist item, classify already-shipped vs missing (components on disk suggest partial completion) — build only the gaps.
2. E3 + E4 test contracts pass 100%, including their [REAL] cases against staging.
3. Every surface in research/11's inventory shows a working thumbs affordance on the running app (Intel, Design chat, each regeneration flow).
4. Thumbs-down opens the reason form and the reason is retrievable via GET /api/feedback/summary.
5. Post-generation review prompt follows the 3-sow-feedback-ux non-blocking pattern (assumed: "pop a review modal" means the approved regenerate-intercept/inline pattern, NOT resurrecting the killed blocking popup — flag if you truly want blocking).

Out of scope / do not touch: E1/E2/E5 (trace naming, nested spans, Langfuse score push) beyond consuming what's merged; no re-planning the package; no changes to the locked SOW feedback UX from 3-sow-feedback-ux.
```
🎯 Target: fable-it — done-ness is already locked in existing epic test contracts (E3/E4), so plan-it would duplicate a finished plan; this is a build-to-DoD run.

💡 Biggest fix: your ask collides with a decision you locked on 2026-07-16 — "pop a review modal after each generation" is the exact popup-on-complete UX that run 3-sow-feedback-ux killed (Matthew Good chicken-and-egg) — so the prompt reframes it as the approved non-blocking pattern and flags the assumption; it also re-anchors the whole ask onto the existing 2-feedback-observability package (W1–W3, epics E3/E4) instead of a from-scratch build.

Setup: run from `/Users/macbook/Workspace/ANM/Beacon/beacon` — the git repo root is the nested `beacon/` dir, not the `ANM/Beacon` folder you named.

Optional refinements (answer and I'll tighten):
1. Review prompt: extend the approved non-blocking pattern (inline box + regenerate-intercept modal), or do you genuinely want a blocking modal after every generation despite the 2026-07-16 decision?
2. Did the earlier "observability-feedback-execution" session already finish E3/E4? If yes, this should route to review-it (verify coverage) instead of fable-it (build).
