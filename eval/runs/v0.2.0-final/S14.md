```
/fable-it

/goal — Surface Beacon's existing AI-feedback stack on EVERY LLM chat surface, so users can rate and explain any generation, not just the SOW flow.

GROUNDING — The feedback stack already exists and is fully wired on the SOW flow (SowEditor/BriefEditor): FeedbackWidget (binary thumbs), RegenerateFeedbackModal (post-generation review), FeedbackReasonForm ("what went wrong" on thumbs-down). This is a REUSE-and-extend job — mount the existing components on the surfaces that lack them. Do NOT rebuild any of the three; do NOT design a new form or modal.

CONTEXT PACKAGE (all under beacon/frontend/src/):
- components/sow/BriefEditor.tsx — the reference integration: wires FeedbackWidget + RegenerateFeedbackModal + SowFeedbackCard together. Mirror this pattern.
- components/FeedbackWidget.tsx — the thumbs component; keys on generationTaskId/traceId + a `feature` string. Reuse verbatim.
- components/FeedbackReasonForm.tsx — the "what went wrong" form; already opens on thumbs-down inside FeedbackWidget. Reuse — do not author a new one.
- components/RegenerateFeedbackModal.tsx — the existing post-generation review modal. Reuse for the "modal after each generation" requirement.
- pages/Intel.tsx:476 — already renders FeedbackWidget; confirm parity, don't duplicate.
- pages/DesignChat.tsx — chat surface that appears to LACK the widget (assumed: primary gap — confirm during discovery).
- services/api.ts → submitFeedback — the endpoint the widget posts to; new surfaces need their own `feature` value.

DoD SKETCH (verification target in brackets):
1. Every LLM chat surface renders FeedbackWidget on each generation — Intel, DesignChat, and every regeneration output. [each surface visibly shows thumbs on a generated answer]
2. Thumbs-down opens the existing FeedbackReasonForm on all newly-wired surfaces. [click 👎 on DesignChat → reason form appears]
3. A post-generation review modal (RegenerateFeedbackModal) fires after generations on these surfaces (assumed: after each generation, reusing the SOW pattern — flag if this should be less frequent/intrusive, e.g. only on regenerate or first-of-session).
4. Ratings + reasons reach the existing submitFeedback endpoint with a distinct `feature` per surface and the correct trace/task id. [feedback row lands with right feature key]

Out of scope / do not touch:
- The SOW flow — already complete; parity reference only, no changes.
- Rebuilding FeedbackWidget / FeedbackReasonForm / RegenerateFeedbackModal — reuse as-is.
- Backend feedback schema — unless a surface needs a new `feature` enum value.
```

🎯 Target: **fable-it** — the pattern already exists and is wired on one flow; this is a bounded, autonomous mirror-onto-remaining-surfaces job with derivable per-surface done-ness (not open-ended enough for plan-it).

💡 Biggest catch: **most of what you asked for already exists.** FeedbackWidget (thumbs), the "what went wrong" form (`FeedbackReasonForm`), and the post-generation review modal (`RegenerateFeedbackModal`) are all built and wired on the SOW flow — Intel already has the thumbs too. The real delta is wiring them onto `DesignChat` + the other regeneration surfaces, so the prompt says *reuse, don't rebuild*.

Two things worth confirming (optional — I flagged them inline so the run proceeds either way):
- **Modal frequency:** after *every* generation everywhere can be intrusive — do you want it that aggressive, or only on regenerate / first-of-session?
- **Surface list:** is `DesignChat` the only chat page missing thumbs, or are there other LLM surfaces beyond Intel/Design/SOW to include?
