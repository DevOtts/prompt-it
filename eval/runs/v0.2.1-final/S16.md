````
/iterate

The tasks-page dropdown e2e is red: opening the dropdown throws "ResizeObserver loop limit exceeded" and the element detaches mid-click (assumed: Playwright-over-CDP suite in the app repo that owns the tasks page — flag if wrong; this compile ran from the prompt-it repo, so no test/component paths could be validated — confirm the failing spec path first).

/goal Make the dropdown e2e pass against the real tasks page by fixing the root cause — not by skipping or weakening the test.

Diagnose before patching — the two symptoms point at different owners:
- Detach-mid-click suggests re-render churn in the dropdown (app-side): find and break the resize→re-render feedback loop.
- "ResizeObserver loop limit exceeded" is often benign browser noise that only reds the suite if the harness promotes page errors to failures. If (and only if) evidence proves it benign here, filter that exact message only — never a blanket error swallow.
Pick from evidence (console/pageerror trace, DOM snapshots around the click), not by default.

DoD sketch:
1. The previously failing dropdown test passes against the live tasks page — the click lands on an attached element.
2. The full e2e suite is green.
3. Green holds across repeated runs — this was flaky, so one pass doesn't prove it.

Out of scope / do not touch: no `test.skip`/`test.fixme`, no retries-as-fix, no blanket page-error suppression, no changes to unrelated tests or components.
````

🎯 Target: iterate — a single fix-test-verify loop with a clear oracle (suite green), run in-session.

💡 Split the one symptom into its two candidate root causes (app-side re-render loop vs. harness treating a benign browser error as fatal) so the session diagnoses instead of blindly suppressing; app repo and spec paths couldn't be validated from here (cwd is the prompt-it skill repo and sibling dirs are sandboxed), so they're flagged assumptions in the block.

Run this from the app repo that owns the tasks page and its e2e suite, not from prompt-it.
