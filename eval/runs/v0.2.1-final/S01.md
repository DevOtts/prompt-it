```
/plan-it

Grounding: The brain platform's tenant integrations settings already have backfill and
webhooks sections, but there is no plug-and-play way for a tenant to install third-party
MCP servers (HubSpot and Airtable both ship official ones), and brain-agent / brain-apps
have no visibility into any installed MCPs.

/goal: Spec the delivery package for tenant-installable MCP servers — an "MCP" section in
integrations settings (install / configure / remove, mirroring the backfill & webhooks
pattern) plus a per-tenant registry that brain-agent and brain-apps read so installed
MCPs' tools are visible and usable at runtime.

Context package (all unvalidated — compiled outside the brain repo; target session must
confirm these paths first):
- brain-agent and brain-apps — the two consumers that must see installed MCPs.
- The existing backfill + webhooks sections in integrations settings — the pattern to
  mirror for UI, per-tenant storage, and credential handling.
- Official HubSpot and Airtable MCP servers — first catalog candidates; their auth models
  (OAuth vs API key) should drive the credential design.

DoD sketch (for plan-it to lock during discovery):
1. A tenant can add, configure, and remove an MCP server — proven on the integrations
   settings page.
2. Installed MCPs persist per-tenant with credentials stored per the existing
   integration-secret conventions — proven in the integrations data store.
3. brain-agent lists and can invoke tools from a tenant's installed MCPs — proven by an
   agent run that uses an installed MCP tool (e.g. HubSpot).
4. brain-apps show which MCPs a tenant has installed — proven on the relevant
   brain-apps view.
5. (assumed: scope covers BOTH a curated catalog (HubSpot, Airtable) and a
   bring-your-own-server-URL option — flag if catalog-only.)

Out of scope / do not touch: authoring our own MCP servers for third parties; existing
backfill/webhook integrations beyond adding the new section; platform-global (non-tenant)
MCP configuration (assumed out — flag if wrong).
```

🎯 Target: **/plan-it** — feature cluster spanning ≥3 surfaces (settings UI, brain-agent, brain-apps) with unresolved scope (install model, credential design, runtime wiring); that discovery is plan-it's job.

💡 This session is sandboxed to the prompt-it repo, so no brain-repo pointer could be validated — every repo reference is marked unvalidated for the target session to confirm, and the two open design questions (catalog vs BYO-URL, tenant-only scope) are carried as inline flagged assumptions rather than blockers.

Run this from the brain system's repo root (or workspace root if brain-agent/brain-apps are separate repos).
