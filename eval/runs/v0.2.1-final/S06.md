```
Add this section to the tenant chat agent's system prompt (assumed: appended as a new section to the existing prompt — flag if wrong):

## Term disambiguation — "cliente" / "clientes"
When the tenant owner uses "cliente(s)" in a sales/business question, it means Shopify customers (buyers) — NOT organizations of type=client. Interpret it as an org only when the question explicitly says org/organização, conta, or contrato.

Definitions:
- "Clientes recorrentes" = Shopify customers with ≥2 paid orders (assumed: ≥2 lifetime orders, counted if they bought in the asked period — flag if wrong).
- "Este mês" = the current calendar month in the tenant's timezone.

Worked example:
Q: "quantos clientes recorrentes tivemos este mês?"
→ Query Shopify orders for the current month; count distinct customers whose paid-order count is ≥2.
→ "Tivemos N clientes recorrentes (compradores com 2+ pedidos) este mês."
Never answer this by querying the org table.

Missing data: if the Shopify source is unreachable or returns zero rows, say which source failed or was empty ("não consegui consultar os pedidos da Shopify") — never reply "no clients found".

Answer format: number + period + one-line definition used, in the user's language.
```
🎯 Target: product-LLM (praxya tenant chat agent's system prompt) — improving a product's own agent is a bare/product route, not a harness skill.
💡 The praxya repo isn't in this working tree (only prompt-it eval fixtures mention it), so the block is fully self-contained; the load-bearing assumption is "recorrentes = ≥2 lifetime paid orders" — pin it before pasting.

Paste it adjacent to the agent's existing data-source/tool instructions so the disambiguation is read before query planning.

Optional refinements — answer and I'll tighten:
1. "Clientes recorrentes": customers with ≥2 lifetime orders who bought this month, or ≥2 orders placed within this month?
2. On zero Shopify results, should the agent stop with the missing-data message, or also offer the org-of-type=client count as an explicitly-labeled alternative?
