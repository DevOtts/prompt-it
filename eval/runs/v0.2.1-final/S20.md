```
/plan-it

Grounding: A review of brain-admin-ui found three locale hardcodes, all previously
"fixed" with inline ternaries on tenant.language — a fix that was reverted as
unmaintainable. The revert was the right call: these are three instances of one
missing mechanism (no tenant-locale layer), not three point defects.

/goal Give brain-admin-ui a single tenant-locale layer — strings, dates, and
currency driven by tenant.language — and migrate the three failing surfaces onto it.

Findings to resolve (seed instances, from the review):
1. Header title hardcoded "Painel" — English tenants see Portuguese.
2. Tasks table dates hardcoded DD/MM/YYYY.
3. Billing widget currency hardcoded "R$".

Context:
- Repo: brain-admin-ui (unvalidated — target session must confirm this path first;
  compiled from outside that tree).
- tenant.language exists as the locale signal (the reverted ternaries keyed off it).
- Whether an i18n library or locale utility already exists in the repo, and how many
  MORE hardcoded pt-BR strings/formats exist beyond these three, is
  (unverified — confirm during discovery). Mechanism choice (existing i18n lib vs.
  message catalog + Intl.DateTimeFormat/NumberFormat wrappers) is discovery's call.

DoD sketch (for the plan to lock):
1. Header title, tasks-table dates, and billing-widget currency each render per
   tenant locale — verified on those three live surfaces for a pt tenant and an
   en tenant.
2. All locale handling lives in the one shared mechanism — verified at the three
   fixed call sites: no per-component locale conditionals remain.

Out of scope / do not touch: inline ternaries on tenant.language are forbidden
(already tried, reverted); no locale-switching UI, no new tenant settings fields,
no sweeping surfaces beyond what discovery explicitly scopes in
(assumed: sweep scope is a discovery decision, not pre-committed — flag if you
want all hardcodes fixed now).
```

🎯 Target: plan-it — three surfaces plus an unresolved mechanism decision (which i18n approach) is scope-shaped work, not a fix loop; the ternary revert proves the point-fix path is dead.

💡 Reframed three point-fixes as one class (missing tenant-locale layer) with the reverted ternary approach pinned as a forbidden failed attempt; brain-admin-ui sits outside this session's allowed tree, so every repo fact is marked unverified for discovery to confirm.

Run the compiled prompt from the brain-admin-ui repo root.

Optional refinements (answer and I'll tighten):
1. Scope: should the plan cover only the three review findings, or a full sweep of every hardcoded pt-BR string/format in brain-admin-ui?
2. Does the review session have a name/ledger card I should back-reference via `/read-chat`, or were the findings only ever pasted here?
