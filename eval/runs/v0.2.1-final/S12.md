```
Create `docs/architecture/overview.md` and document the eval suite under `eval/` in it. (assumed: creating the file is intended — `docs/architecture/` does not exist in this repo yet; flag if the doc actually lives elsewhere)

Ground the doc in the real files — read each before describing it, don't invent behavior:
- `eval/scripts/run-eval.sh` — how an eval run is executed
- `eval/sample-prompts.md` + `eval/expected-prompts.md` — inputs vs expected outputs
- `eval/fixtures/` — supporting fixtures
- `eval/runs/` — where results land (most recent: `eval/runs/v0.2.1-final/`)

If a file's purpose isn't clear from its contents, write "purpose unclear" in the doc instead of guessing.

Done when: `docs/architecture/overview.md` exists, covers all four `eval/` areas above, and every path it cites resolves on disk (`ls` spot-check).

Out of scope / do not touch: the `eval/` files themselves, `SKILL.md`, `plugins/`, `README.md`.
```
🎯 Target: bare Claude Code session — single-file docs edit; no *-it machinery needed.

💡 Fixed a dead pointer: `docs/architecture/overview.md` (and the whole `docs/architecture/` dir) doesn't exist — `docs/` only holds `research-SYNTHESIS.md`, `v0.2.1-eval-fixes-KICKOFF.md`, and `analysis-example/` — so the prompt creates it and grounds the doc in the verified `eval/` contents.

Optional refinements (answer and I'll tighten):
1. Should this live in a new `docs/architecture/overview.md`, or folded into the existing `README.md` / `docs/v0.2.1-eval-fixes-KICKOFF.md`?
2. Should the doc also explain how `eval/runs/v0.2.1-final/` maps to the recent S11/S13/S14/S18 eval-fix commits, or stay a pure structural overview?
