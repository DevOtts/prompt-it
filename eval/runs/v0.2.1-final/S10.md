```
Fix the typo "recieve" → "receive" in eval/fixtures/notes.md (line 2, sole occurrence). Change only that word — touch nothing else in the file. When done, show the corrected line. If "recieve" isn't found at that line, stop and report instead of editing elsewhere.
```

🎯 Target: bare session — a one-word edit needs no harness skill; any plain session executes it in one turn.

💡 Validated the pointer: `recieve` exists exactly once, at `eval/fixtures/notes.md:2` — pinned the line and occurrence count so the session edits deterministically and can't over-match.

Run from the prompt-it repo root.
