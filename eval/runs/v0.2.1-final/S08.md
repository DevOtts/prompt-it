```
/fable-it

Continuation of the trace-naming work — review results are in.

Verified working (do not re-fix): traces originating from chat arrive in Langfuse
named "chat" — confirmed in Langfuse project `praxya`.

Gaps — both instances of one class: call sites that bypass the origin-naming
mechanism chat now uses.
1. `brain-apps/pulse` has 3 LLM call sites still emitting bare "llm.generate"
   traces — never given origin names. (path per review — unvalidated from this
   tree; confirm it first)
2. Automation-triggered LLM calls also arrive unnamed.

Prefer the class fix over point patches: extend the same mechanism that made
chat work (assumed: a shared wrapper around llm.generate owns the trace name —
flag if naming is per-call-site) so an origin without a name is defaulted or
caught, then name the known gaps.

/goal Every LLM call reaching Langfuse project `praxya` carries its origin name —
no bare "llm.generate" traces remain.

DoD sketch:
1. The 3 brain-apps/pulse call sites produce origin-named traces — visible in
   Langfuse project `praxya`.
2. Automation-triggered calls produce origin-named traces — visible in `praxya`.
3. No new bare "llm.generate" traces appear in `praxya` (sweep for call sites
   the review may have missed).

Out of scope / do not touch: the chat naming path (verified working); trace
payloads/content — naming only.
```
🎯 Target: fable-it — undisputed fix + verify across two surfaces with done-ness derivable from Langfuse; too wide for a single /iterate loop, no unresolved scope needing plan-it.

💡 Generalized the two FAILs to their shared class (call sites bypassing the naming mechanism) and added DoD item 3 as a no-bare-traces sweep so origins beyond the 3 known sites get caught — drop item 3 if you want only the enumerated fixes. `brain-apps/pulse` sits outside this working tree, so the pointer is carried unvalidated.

Run from the repo that contains `brain-apps` (this session's tree is the prompt-it repo — the path couldn't be validated from here).
