```
/review-it

/goal Confirm or refute the team's claim that the feedback thumbs up/down component is finished AND deployed to the test environment — this verdict gates what I tell the client, so it must be evidence-backed, not report-trusted.

Claim under test: "feedback thumbs up/down component finished and deployed to test env", as stated in the last session's report (assumed: the product repo's latest `.agents/history/INDEX.md` card and the run report it points at, e.g. `.fable-it-reports/report.md` — unvalidated from the compiling session; confirm this path first, flag if wrong). Verify against that report's own stated done-criteria/test contract — do not invent new criteria. Both halves of the claim count: built, and live in the test env.

Out of scope / do not touch: no fixes, no code changes, no redeploys — verdict plus evidence only, including an honest list of anything the report claims that could not be reproduced.
```
🎯 Target: review-it — "check it's actually done before I tell the client" is a verification ask over an existing claim, review-it's exact job.
💡 Validated the claim's location and it is NOT in this repo: prompt-it's reports and ledger have zero mentions of the feedback component (the only thumbs-up/down text here is `docs/prompt-examples.txt`, an example prompt, not a report) — so the report pointer is marked unvalidated and the review must confirm it first.

Run this from the product repo where the component lives, not from prompt-it (this session is sandboxed to prompt-it and could not see sibling repos).

Optional refinements — answer and I'll tighten the prompt:
1. Which repo hosts the component and the session report — is it one of your client-product repos (the Brief/SOW/Plan generator described in `docs/prompt-examples.txt`) or another Devotts project?
2. Where does that project keep its session report — a `.fable-it-reports/report.md`-style run report, or the latest `.agents/history/` ledger card?
