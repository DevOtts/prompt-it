```
Write and run a script that dedupes `data/customers.csv` by email and reports how many rows were removed.

Requirements:
- Python 3, stdlib only (assumed: Python — flag if you want bash/other).
- Duplicate = same email compared case-insensitively; keep the first occurrence (assumed — flag if last-wins or a merge is wanted).
- Assumes a header row with an `email` column (flag if the column is named differently or there's no header).
- Back up the original to `data/customers.csv.bak`, then overwrite `data/customers.csv` with the deduped rows (assumed in-place — flag if you want a separate output file instead).
- Print a report: rows read, duplicates removed, rows remaining.
- If the file is missing, unreadable, or has no email column: stop and report the problem — do not guess or emit a partial file.

Verify by running the script and showing its output, plus `wc -l` on the backup and the deduped file.

Out of scope / do not touch: anything outside `data/` and the new script file; no third-party packages; no git commits.
```
🎯 Target: bare Claude session — scratch repo with no plugins means no harness skills; bare route gets the uncertainty clause, output contract, and scope fence inline.
💡 Added the run-and-show verification, missing-data behavior, and backup-before-overwrite, and flagged three assumptions (keep-first wins, `email` header column, in-place dedupe) — `data/customers.csv` is in your scratch repo so I couldn't validate it from here; the prompt makes the target session fail loudly if it's absent.

Run the prompt from the scratch repo root.

Optional refinements (answer and I'll tighten):
1. Keep-first or keep-last when emails collide?
2. Overwrite `data/customers.csv` (with `.bak`) or write to a new `data/customers.deduped.csv`?
