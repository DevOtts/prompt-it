```
/iterate

/goal Make `pnpm build` exit green.

Failing error: `src/eval/runner.ts(42,18): error TS2345: Argument of type 'string | undefined' is not assignable to parameter of type 'string'.`

Fix the type error at its source — narrow or explicitly handle the `undefined` case; no `!` assertions or `as string` casts unless the value is provably present at that point. Do not loosen tsconfig strictness to make the error disappear.

Verify: rerun `pnpm build` until it exits 0. If fixing line 42 surfaces further errors, fix those too — done means a clean build, not a clean line 42.

Out of scope: no refactors beyond what the type fix requires; do not touch build config.
```
🎯 Target: /iterate — a single fix-test-verify loop with an unambiguous oracle (`pnpm build` exit 0).
💡 Pointer unvalidated: this working tree (`prompt-it`) has no `src/eval/runner.ts` and no pnpm project — the path is carried verbatim from your error output (assumed: you'll run this in the repo where the build fails — flag if wrong).

Run the prompt from the repo root that contains `src/eval/runner.ts`, not from `prompt-it`.
