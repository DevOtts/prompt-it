```
/fable-it

/goal Ship the LLM key-source fix in brain-api-core to done: the ontology client must resolve its LLM API key from the per-Space backbone key, falling back to the env key when the Space has none — the same resolution pattern trigger-draft.service.ts already implements.

Context (read first):
- brain-api-core repo (unvalidated — this compile ran outside that tree; confirm the repo path before starting)
- trigger-draft.service.ts — the pattern to mirror: it already does per-Space backbone key with env fallback
- ontology client (assumed: the module in brain-api-core that makes the LLM calls for ontology work — flag if wrong)

DoD:
1. Ontology client key precedence matches trigger-draft.service.ts: Space backbone key when set, existing env key otherwise.
2. Space WITH a backbone key → ontology calls use that key (proven by the key-resolution test for that path).
3. Space WITHOUT a backbone key → env fallback used, no error (proven by its companion test).
4. Existing suite green; both resolution branches covered.

Out of scope / do not touch: trigger-draft.service.ts itself, key resolution in any other service, no new env vars — reuse the existing `[ENV_VAR_NAME]` fallback.
```

🎯 Target: fable-it — "ship to done" with derivable done-ness is an autonomous goal-to-DoD delivery run.

💡 Dropped your teams/model-tiering instruction from the prompt on purpose: fable-it Step 3 owns delegation and model economics canonically — routing to fable-it IS that decision, and restating it in the prompt would conflict with its own tiering logic. Both repo pointers are marked unvalidated (this session is sandboxed to the prompt-it tree and couldn't confirm them).

Run the prompt from the brain-api-core repo root.
