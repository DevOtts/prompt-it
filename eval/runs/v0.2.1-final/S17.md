```
Organize ~/Downloads by moving files into type subfolders. Do not delete or overwrite anything.

Goal: every matching file in the top level of ~/Downloads ends up in one of four subfolders, created there if missing:
- Images/ — png, jpg, jpeg, gif, webp, heic, svg, tiff, bmp
- PDFs/ — pdf
- Archives/ — zip, tar, gz, tgz, bz2, xz, 7z, rar
- Installers/ — dmg, pkg, app, iso, deb, exe, msi

Rules:
1. Top level of ~/Downloads only — never descend into or move existing subfolders (assumed: leave existing folders untouched — flag if wrong).
2. Move, never copy-then-delete; never delete anything under any circumstance.
3. Name collision at the destination: rename the incoming file with a " (2)" / " (3)" suffix — never overwrite.
4. Files matching none of the four categories stay where they are, untouched (assumed: no "Other" folder — flag if wrong).
5. Match extensions case-insensitively (.PDF, .JPG).
6. Dry-run first: print the planned moves as a table (filename → destination), then execute.
7. If any single move fails, skip it, continue, and list every skipped file at the end.

When done, show: count of files moved per folder, the skipped/unmatched list, and the output of `ls ~/Downloads` so I can see the result.
```

🎯 Target: bare Claude Code session (no plugins) — user named the target explicitly; simple single-scope agentic task, no harness skill needed.

💡 Added the safety rails a bare session doesn't get for free: top-level-only scope lock, collision-rename instead of overwrite, dry-run-before-execute, and unmatched-files behavior — plus concrete extension lists so the agent doesn't invent its own taxonomy. `~/Downloads` couldn't be validated from this repo's sandbox (kept verbatim); the target session must be allowed to access it — start that session with `claude --add-dir ~/Downloads` or approve the permission prompt when it appears.

Optional refinements (answer and I'll tighten):
1. Unmatched files (e.g. .csv, .docx, .mp4): leave in place as written, or sweep into an `Other/` folder?
2. Should `.app` bundles and mounted `.dmg` leftovers count as Installers, or is that list only for the four you named plus my extension guesses?
