```
/fable-it

Grounding: the ontology OpenRouter client in brain-api-core resolves its API key from the global pod env var only, ignoring the Space's configured backbone connection. trigger-draft.service.ts already implements the correct resolution — mirror it, don't reinvent it.

/goal Make the ontology OpenRouter client resolve its API key per-Space, using the same chain trigger-draft.service.ts already uses, and ship it.

Context (read in order):
- brain-api-core repo (unvalidated — target session must confirm this path first; assumed: the ontology client is a single module there — locate it before editing)
- trigger-draft.service.ts — the pattern to imitate; it already solves per-Space key resolution
- Resolution chain (carried from author): per-Space "Direct (OpenRouter)" backbone connection key → `OPENROUTER_API_KEY` → `OPENAI_API_KEY`

DoD:
1. Ontology client uses the Space's "Direct (OpenRouter)" backbone connection key when one is configured — verification target: the ontology-generation flow for a Space that has such a connection.
2. Fallback order `OPENROUTER_API_KEY` then `OPENAI_API_KEY` when no per-Space connection exists — verification target: same flow on a Space without one (current behavior unbroken).
3. Shipped through the repo's normal build/deploy pipeline (assumed: brain-api-core has one — flag if ship means something else here).

Out of scope / do not touch: trigger-draft.service.ts itself (read-only reference), backbone-connection management UI/schema, other consumers' key resolution.
```

🎯 Target: fable-it — deliverable goal with derivable done-ness plus "build and ship" = autonomous run to DoD, not planning or review.

💡 Cross-tree pointers (brain-api-core, trigger-draft.service.ts) couldn't be validated — this session is sandboxed to the prompt-it repo — so they're marked for the target session to confirm; the exact key-resolution chain is spelled out in the block so nothing depends on the author's head.

Run the prompt from the brain-api-core repo root, not from prompt-it.
