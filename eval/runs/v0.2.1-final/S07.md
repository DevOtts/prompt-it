````markdown
/fable-it

Iteration 2 of the observability epic — review round 1 findings below.

**Verified working (build on it, don't re-touch):** Langfuse deployed globally, per-tenant projects live, chat traces arriving — verified in the Langfuse UI, project `praxya`.

/goal Close the two review gaps: feedback capture on every LLM surface, and step-level cost visibility in intel traces.

**Gap 1 — feedback coverage.** The thumbs component renders only on the chat page; the Intel and Design pages have LLM interactions with no feedback UI. This is a class problem, not two point fixes: feedback is wired per-page instead of per-LLM-interaction. Generalize the existing chat-page component so any LLM interaction surface gets it, then attach it to Intel and Design. (assumed: Intel + Design are the only uncovered LLM surfaces — the review names only these two; if the run finds others, flag them rather than silently expanding.)

**Gap 2 — flat traces.** Multi-step intel generation emits a single flat `llm.generate` trace. Restructure the instrumentation so it emits a parent trace with one child span per step, each carrying its own token/cost figures.

DoD:
1. Intel page: thumbs render on LLM outputs; a submitted rating appears in Langfuse (project `praxya`) attached to that interaction's trace.
2. Design page: same as 1.
3. Chat page feedback still works — no regression.
4. A multi-step intel generation shows in the Langfuse UI as nested per-step spans with per-step cost, not one flat `llm.generate`.

Out of scope / do not touch: Langfuse deployment/infra, per-tenant project provisioning, and the chat tracing pipeline — all verified working in round 1.
````

🎯 Target: fable-it — both gaps are review-verified fixes with derivable done-ness (the review already did the scope discovery); fix + verify in one autonomous run.

💡 Reframed gap 1 class-over-instance (per-LLM-interaction mechanism, not two page patches) and flagged the assumption that Intel + Design are the only uncovered surfaces — the review names only those two. No review-it report or ledger card for the review session was found on disk, so the findings are embedded verbatim instead of a `/read-chat` back-reference.

Run from the product repo root (path unvalidated from this repo — this session is in the prompt-it skill repo, not the praxya codebase).
