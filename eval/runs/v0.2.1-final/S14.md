```
/plan-it

Beacon already has message-level feedback machinery — `FeedbackWidget.tsx`,
`RegenerateFeedbackModal.tsx`, and `FeedbackReasonForm.tsx` exist under
`beacon/frontend/src/components/`, and `Intel.tsx` + `DesignChat.tsx` already
reference feedback — so per-surface coverage is partially built. Which chat/
generation surfaces still lack it is unresolved (confirm during discovery).

/goal Every surface where a user chats with the LLM or triggers a
generation/regeneration collects feedback the same way — thumbs on each
response, a review modal after each generation, and a "what went wrong" form
on thumbs-down — so we get usable signal on where the LLM underperforms.

Context (repo: /Users/macbook/Workspace/ANM/Beacon — app code lives in the
nested `beacon/` directory):
- beacon/frontend/src/components/FeedbackWidget.tsx — existing thumbs widget; the pattern to extend
- beacon/frontend/src/components/RegenerateFeedbackModal.tsx + FeedbackReasonForm.tsx — existing modal + reason form; discovery decides reuse vs. new "what went wrong" form
- beacon/frontend/src/pages/Intel.tsx, DesignChat.tsx — the two named target pages; both already contain feedback references
- .taskstate/feedback-obs-grounding.md — prior run's grounding: feedback rows land in `llm_feedback` keyed on trace_id; thumbs-down persists dimensions + comment
- .taskstate/sow-fb-ux-decisions.md — locked UX decisions from the SOW feedback run; read before re-deciding UX

DoD sketch (for plan-it to lock):
1. Inventory of every LLM-chat/generation surface (Intel, Design, all regeneration flows), each marked has/lacks thumbs — the rendered pages prove it
2. Thumbs on every inventoried surface produce `llm_feedback` rows — the table shows them
3. A review modal appears after each generation/regeneration on those surfaces
4. Thumbs-down opens a "what went wrong" form whose submission lands in `llm_feedback` with the reason
5. (assumed: reuse the existing widget/modal components rather than build new ones — flag if wrong)

Out of scope / do not touch: feedback analytics/reporting dashboards, the SOW
editor's existing feedback flow (extend from it, don't redesign it), backend
feedback schema changes beyond what the form itself needs.
```

🎯 Target: **plan-it** — "extend X to every page" across ≥3 surfaces is unresolved scope; discovery must map what's already covered before anything is built.

💡 Lookups show this feature is **partially built already**: `RegenerateFeedbackModal.tsx` and `FeedbackReasonForm.tsx` exist, and Intel + DesignChat already reference feedback — so the prompt frames the job as a coverage-gap inventory, not greenfield. Also fixed a path trap: the app code is one level down, in `Beacon/beacon/`, not the repo root you gave.

Run plan-it from `/Users/macbook/Workspace/ANM/Beacon`.
