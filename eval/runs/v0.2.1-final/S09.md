```
/fable-it

Continuation of the Airtable write feature after a failed review. What stands: the write
feature and its CDP e2e suite exist and run, and once the integration is enabled the UI
save path completes (downstream unverified). Nothing else is trusted — both green claims
were contested by a human run.

/goal Make the Airtable write feature provably work end-to-end for a human — tasks
dropdown error-free, saved record visible in Airtable — and make the e2e suite incapable
of reporting green while the UI throws.

Findings to close (review left no report file; findings carried inline):
1. FALSE GREEN — CDP e2e passed while the tasks dropdown throws UI errors under a human
   run. Treat as a class defect, not one bad test: the suite's oracle never observes
   page/console errors, so any UI exception passes silently. Fix that mechanism in the
   suite first, then the dropdown errors it was hiding.
2. UNVERIFIED SYSTEM OF RECORD — saving to tasks required enabling the integration first,
   and nobody confirmed the record landed in Airtable. "Saved" means: record present in
   the Airtable tasks table (assumed: enabling the integration during setup is an
   acceptable precondition, not itself the bug — flag if it should work without it).

Already tried, do NOT re-propose: re-running the CDP suite with longer waits — same
false green.

DoD sketch:
1. Tasks dropdown operates with zero UI/console errors in the running app, not just in
   the suite.
2. A task saved through the UI appears as a record in the Airtable tasks table (assumes
   authenticated Airtable access via the project's existing [AIRTABLE_API_KEY]-style env).
3. The e2e suite fails when the page throws — shown against the pre-fix dropdown error.
4. The integration-enable precondition is handled explicitly in test setup or documented.

Out of scope / do not touch: other Airtable-backed features, unrelated e2e specs, the
integration enable/disable UI beyond what DoD 4 needs.
```
🎯 Target: fable-it — both findings need fixes plus fresh verification (a false green and an unchecked system of record), not just a re-review.
💡 No review report file or ledger card exists for this review (checked `.fable-it-reports/` and `.agents/history/INDEX.md`), so the findings are carried inline; the failed longer-waits attempt is pinned so the next session can't re-propose it.

Run from the repo that owns the Airtable feature — it is not the current `prompt-it` tree.

Optional refinement: which repo owns the feature — `~/Workspace/Devotts/Ottimus` (the only Devotts tree matching "airtable", though it has no obvious tasks-dropdown UI) or a repo outside `~/Workspace/Devotts`? Answer and I'll pin the path plus validated pointers into the block.
