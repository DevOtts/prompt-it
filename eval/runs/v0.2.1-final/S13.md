```
/iterate

Auth bug in the Engine workspace: authenticated sessions expire well before their intended lifetime — users are logged out too early. (assumed: repo lives at ~/Workspace/Devotts/… under the name "Engine" — confirm the exact path before starting; this prompt was compiled outside that tree and could not validate it.)

/goal Users stay logged in for the full intended session lifetime; find the root cause of premature session expiry in Engine's auth layer and fix it.

Start by locating the session lifetime configuration (token/cookie TTL, refresh logic, idle timeout) — the bug is most likely a mismatch between where the session is issued and where it is validated. (assumed: "sessions" means end-user auth sessions, not DB/worker sessions — flag if wrong.)

DoD sketch:
1. Root cause named as file:line, with a one-line explanation of why sessions were dying early.
2. A logged-in session survives past the window where it previously died — proven against the running app's auth endpoint or login flow, not just unit tests.
3. Existing auth test suite passes.

Out of scope / do not touch: auth provider swaps, session storage backend changes, or any refactor beyond the minimal fix — if the root cause demands one of those, stop and report instead of proceeding.
```

🎯 Target: **iterate** — a single known-bug fix-test-verify loop, not a multi-surface scope or an overnight run.

💡 The "Engine workspace" path could not be validated from this session (sandbox is locked to the prompt-it repo), so it's carried as an explicitly unvalidated assumption the target session must confirm first — also pinned that "sessions" means user auth sessions and fenced off provider/storage rewrites so a "fix" doesn't become a migration.

Run this from the Engine repo root, not from prompt-it.

Optional refinements (answer and I'll tighten the prompt):
1. Do sessions die mid-activity (idle timeout too aggressive) or at a fixed early moment regardless of activity (absolute TTL/refresh misconfigured)?
2. Is there a target lifetime to hit (e.g. "stay logged in 24h" vs "until browser closes")?
