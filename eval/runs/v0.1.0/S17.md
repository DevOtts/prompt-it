Routed to **bare** — plain Claude Code, no plugins, so this prompt has to carry the pieces a harness skill would normally own (uncertainty fallback, output contract, stop/scope guardrails for a file-moving agent). I couldn't `ls ~/Downloads` from this sandboxed session (blocked outside this repo's working directory), so the prompt states its file-type assumptions explicitly rather than presenting them as verified.

```
Organize my ~/Downloads folder by moving files into type-based subfolders. Do not delete anything, ever.

Goal: every top-level file in ~/Downloads that matches one of the categories below ends up in its subfolder; everything else stays exactly where it is.

Categories and extensions (assumed — edit this list first if you use different conventions):
- Images/: jpg, jpeg, png, gif, webp, heic, svg, bmp, tiff
- PDFs/: pdf
- Archives/: zip, tar, gz, tgz, rar, 7z
- Installers/: dmg, pkg, exe, msi

Steps:
1. List the top-level files currently in ~/Downloads (not files already inside subfolders — leave existing subfolders and their contents untouched).
2. Build a move plan: file → destination subfolder, based on the category list above. Files whose extension isn't in any category list stay in place — do not invent an "Other" catch-all.
3. Print the full plan (source → destination) and wait for my go-ahead before moving anything.
4. On confirmation, create ~/Downloads/Images, /PDFs, /Archives, /Installers as needed and move (not copy) each matched file. If a destination filename already exists, do not overwrite — append " (2)", " (3)" etc. to the moved file instead.
5. Report: how many files moved into each subfolder, how many were left untouched at the root and why (unmatched extension), and confirm the total file count before == total file count after (nothing lost, nothing deleted).

If a file extension is ambiguous or you're unsure which category it belongs to, leave it at the root rather than guessing, and list it in the report as "needs manual sort."

Out of scope / do not touch: files already inside any existing subfolder of ~/Downloads, files outside ~/Downloads, and no renaming beyond the collision-suffix rule in step 4. Never delete a file under any circumstance.
```

🎯 Target: bare (plain Claude Code, no plugins) — file-system task with no repo/spec, so none of the *-it harness skills fit
💡 Added the pieces a bare session needs that you didn't specify: an explicit extension-to-category mapping (marked assumed, since ".dmg" alone is ambiguous between archive/installer), a collision-safe rule so "delete nothing" also means "never silently overwrite," a preview-before-move confirmation step, and a before/after file-count check so you can verify nothing vanished.
