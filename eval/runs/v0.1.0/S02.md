```
/fable-it

The ontology OpenRouter client in brain-api-core resolves its API key from the global pod env var only, ignoring per-Space "Direct (OpenRouter)" backbone connections — so a Space with its own OpenRouter connection configured still authenticates against the pod-wide key instead of its own. trigger-draft.service.ts in the same repo already implements the correct resolution order for this exact key.

Goal: make the ontology OpenRouter client resolve its API key in this order — (1) the calling Space's "Direct (OpenRouter)" backbone connection key, (2) OPENROUTER_API_KEY, (3) OPENAI_API_KEY — matching trigger-draft.service.ts's existing precedence exactly, then ship it.

Context:
- Pattern to imitate: `trigger-draft.service.ts` (unvalidated — target session must confirm this path first; not reachable from the current working tree). Mirror its resolution order; if it exposes a shared helper/service for this lookup, reuse that helper rather than reimplementing the fallback chain in the ontology client.
- Target: the ontology OpenRouter client in brain-api-core (unvalidated — exact file/class to be located by the target session).

Definition of Done:
1. Per-Space "Direct (OpenRouter)" connection configured → that connection's key is used for the ontology client's OpenRouter calls, not the pod env var — verification target: the outbound OpenRouter request/auth header for that Space's ontology flow.
2. No per-Space connection configured → falls back to OPENROUTER_API_KEY — verification target: same flow, Space without a connection.
3. OPENROUTER_API_KEY unset → falls back to OPENAI_API_KEY — verification target: same flow with OPENROUTER_API_KEY unset.
4. Resolution order/mechanism is identical to trigger-draft.service.ts's precedence for these three sources — no divergent logic between the two clients.

Out of scope / do not touch: trigger-draft.service.ts itself (reference only, already correct) — do not modify it; do not change key resolution for any client/service other than the ontology OpenRouter client; do not change the backbone-connection schema or add new connection types.
```

🎯 Target: fable-it — scoped fix with a clear pattern to imitate and a "build and ship" ask; checkable DoD, autonomous build-to-ship run.
💡 `brain-api-core` isn't in this session's working tree, so I couldn't verify `trigger-draft.service.ts` or locate the ontology client myself — both are flagged unvalidated for the target session to confirm first (fable-it's own discovery step owns that, not this compile).

Setup: run from the `brain-api-core` repo root.
