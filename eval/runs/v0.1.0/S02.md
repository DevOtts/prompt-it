This ask is scoped and specific but needs the repo/file context packaged for a target session — and I hit a hard constraint: this session is sandboxed to `prompt-it` only, so I cannot `ls`/`grep` into `brain-api-core` to validate the `trigger-draft.service.ts` pointer or locate the ontology client file. I'm compiling the prompt to be self-discovering instead of asserting unverified paths.

Routing: **fable-it** — you said "build and ship it," which is fable-it's own trigger language (build to completion including deploy-verify), not a same-session `iterate` loop.

```
/fable-it

Fix the ontology OpenRouter client in brain-api-core: it currently reads
the API key from the global pod env var instead of resolving it per-Space.

/goal: the ontology OpenRouter client resolves its API key the same way
trigger-draft.service.ts already does — per-Space "Direct (OpenRouter)"
backbone connection key first, then OPENROUTER_API_KEY, then
OPENAI_API_KEY — instead of reading the global pod env var.

Context:
- Reference implementation: trigger-draft.service.ts already implements
  this exact precedence chain. Locate it first (grep the repo) and mirror
  its resolution logic/order exactly — do not reinvent the fallback chain.
- Target file: the ontology OpenRouter client (exact path not yet
  confirmed this session — locate via grep for the client's current
  direct env-var read, e.g. OPENROUTER_API_KEY/OPENAI_API_KEY usage
  outside a per-Space lookup).

Definition of Done:
1. Ontology client's key-resolution order matches trigger-draft.service.ts's
   implementation — verify by diffing the two resolution code paths.
2. When a Space has the "Direct (OpenRouter)" backbone connection
   configured, the client uses that Space-scoped key, not the pod env var
   — verify with a Space that has the connection configured and a
   deliberately different/invalid pod-level env var, confirming the
   outbound request uses the Space key.
3. When no Space connection is configured, falls back to
   OPENROUTER_API_KEY — verify by unsetting the Space connection and
   confirming that env var is used.
4. When neither the Space connection nor OPENROUTER_API_KEY is set, falls
   back to OPENAI_API_KEY — verify the same way with both unset.
5. Existing ontology client tests/callers still pass — no regression.

Out of scope / do not touch: do not modify trigger-draft.service.ts itself
(reference only); do not extend this fix to other OpenRouter clients even
if they share the same defect — flag them instead of fixing silently; no
broader refactor of Space/backbone-connection resolution beyond what this
fix needs.
```

🎯 Target: fable-it — "build and ship it" is a full build-to-completion+deploy ask, not a single same-session fix loop.

💡 I could not validate the `brain-api-core` repo path or locate `trigger-draft.service.ts`/the ontology client from this session (sandboxed to `prompt-it` only) — the prompt directs the target session to grep for both rather than trusting an unverified path.

Run this from a fresh session opened at the `brain-api-core` repo root.
