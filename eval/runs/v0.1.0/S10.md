Pointer confirmed: `eval/fixtures/notes.md` exists, single occurrence of "recieve" at line 2. This is clarity-gate territory — clear, scoped, single-file typo fix — so a minimal tighten is all it needs, no full 6-slot draft.

```
Fix the typo "recieve" → "receive" in eval/fixtures/notes.md (line 2, in "we recieve the payload"). Single-word correction only — no other edits to the file.
```

🎯 Target: bare/direct edit — trivial single-file, single-occurrence fix; doesn't warrant iterate's fix-test-verify loop
💡 Validated the pointer myself: file exists, exactly one match of "recieve" at line 2 — prompt cites the exact line so the next session doesn't need to re-grep
