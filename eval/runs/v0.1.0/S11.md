```
Author CONTRIBUTING.md in the repo root for prompt-it (this repo — /Users/macbook/Workspace/Devotts/prompt-it).

/goal: Document how to add a new target profile to references/targets.md, and link that doc from README.md.

Context:
- @plugins/prompt-it/skills/prompt-it/references/targets.md — the file being documented. Each existing profile (plan-it, fable-it, review-it, iterate, bare, product-LLM) follows a fixed template: a `## <target name>` heading, then **PARSES → include**, **OWNS → omit**, **Cautions** bullets — and every profile also gets one row added to the "Quick omission table" near the bottom (line 46). Mirror this exact structure in the contributing steps rather than inventing a new format.
- @README.md — "Development notes" section (~line 288) is the natural anchor for a link to CONTRIBUTING.md; its existing prose already points readers to research-SYNTHESIS.md §3.0 for the design-center boundary rule ("know the target, include what it parses, omit what it owns") — CONTRIBUTING.md should cross-reference that rule, not restate it.

DoD sketch:
1. CONTRIBUTING.md exists at the repo root and contains a numbered walkthrough for adding a new target profile: writing the PARSES/OWNS/Cautions block, and adding the matching row to the Quick omission table in targets.md.
2. README.md links to CONTRIBUTING.md (Development notes section is the suggested anchor point — verify with `grep -n CONTRIBUTING.md README.md`).

Out of scope / do not touch:
- No CI changes.
- CONTRIBUTING.md covers ONLY the target-profile-addition workflow — not a general PR/contribution process, code style guide, or testing instructions (those aren't in scope unless the profile-addition steps require touching SKILL.md, which they don't here).

If the exact README insertion point is ambiguous, add a one-line pointer near Development notes rather than restructuring the file (assumed: root-level CONTRIBUTING.md is correct per GitHub convention — flag if this repo expects it elsewhere).

When done: confirm the file renders correctly and the README link resolves to it.
```

🎯 Target: bare Claude Code session — this is a single bounded documentation task with a fully specified DoD already; plan-it's discovery/spec pipeline (research fan-out, PRD/epic authoring) would be pure overhead here.

💡 Overridden your suggested routing from plan-it to a direct/bare session — plan-it is built for fuzzy or large ideas needing discovery, and this ask already has a locked scope and DoD, so routing it there would trigger unnecessary process. Also validated both pointers: `references/targets.md` lives at `plugins/prompt-it/skills/prompt-it/references/targets.md` (has a per-profile template + a "Quick omission table" your contributing steps should mirror), and README.md's "Development notes" section (~line 288) is the natural link anchor.
