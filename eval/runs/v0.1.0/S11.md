Both pointers check out: `plugins/prompt-it/skills/prompt-it/references/targets.md` exists (the profile-addition target), `README.md` exists, and `CONTRIBUTING.md` doesn't exist yet — consistent with the DoD's "file exists" starting state.

This ask is already clear, scoped, and explicitly routed, so this is a minimal-tighten pass, not the full pipeline — packaged into what `plan-it` parses (goal, context pointers, light DoD signal, scope fence), omitting what it owns (DoD lock, sizing, discovery).

```
/plan-it

Documenting the contribution process for adding a new target profile to prompt-it's per-target ledger.

/goal: add a CONTRIBUTING.md to the repo root that documents the steps to add a new target profile to references/targets.md, and link it from README.md.

Context:
- @plugins/prompt-it/skills/prompt-it/references/targets.md — the file new profiles get added to; base the steps on its existing structure (PARSES → include / OWNS → omit / Cautions, plus the quick omission table at the bottom that also needs a new column per profile)
- @README.md — has an "Architecture / What's inside" section (lists targets.md's role) and a footer links row (License + links) — either is a natural place to add the CONTRIBUTING.md link

DoD sketch (light — plan-it locks the binding version):
1. CONTRIBUTING.md exists at repo root with profile-addition steps — verify: the file itself
2. README.md links to it — verify: a working relative link in README.md

Out of scope / do not touch: no CI changes.
```

🎯 Target: plan-it — you named it explicitly; task is well-scoped already so plan-it's job here is mainly to lock the binding DoD and size the CONTRIBUTING.md content, not run discovery
💡 Validated both pointers exist on disk (targets.md and README.md) and confirmed CONTRIBUTING.md doesn't already exist — matches your DoD's "file exists" as a target state, not already true.
