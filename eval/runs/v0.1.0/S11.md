All pointers validated: `README.md` has a "Development notes" section and a "License + links" list (with `CHANGELOG.md`/`LICENSE` links) — either is a natural anchor for a `CONTRIBUTING.md` link. `plugins/prompt-it/skills/prompt-it/references/targets.md` exists and is the file the new profile-addition steps must document. `CONTRIBUTING.md` doesn't exist yet (confirmed, it's the deliverable). Route is locked by the user to `plan-it`, though this reads as a small single-deliverable task — noted below rather than overridden.

```
/plan-it

This is the prompt-it repo's own root (not a target-project repo) — README.md
already documents the *-it family and links out via a "License + links" /
"Development notes" section, but there's no CONTRIBUTING.md yet, so anyone
wanting to add a new target profile has no documented on-ramp.

/goal Add a CONTRIBUTING.md to this repo that documents how to add a new
target profile to plugins/prompt-it/skills/prompt-it/references/targets.md,
and link it from README.md.

Context:
- plugins/prompt-it/skills/prompt-it/references/targets.md — the file being
  documented; each existing profile follows a PARSES→include /
  OWNS→omit / Cautions structure (see the plan-it and fable-it entries) —
  the new CONTRIBUTING steps should teach a contributor to match this shape.
- README.md — has a "Development notes" section (line 288) pointing
  contributors to research-SYNTHESIS.md's boundary audit, and a "License +
  links" section (line 292) listing CHANGELOG.md/LICENSE — either is a
  natural place to add the CONTRIBUTING.md link; pick whichever reads better
  once the file exists.

Intent (plan-it locks the binding DoD in its own Phase 1 — this is a light
sketch, not a binding contract):
1. CONTRIBUTING.md exists at repo root with concrete steps for adding a new
   target profile to targets.md (what sections/fields a profile needs, the
   PARSES/OWNS/Cautions convention to follow, where in the file to add it).
2. README.md links to CONTRIBUTING.md.

Out of scope: no CI changes. Do not edit targets.md's existing profile
content — this is documentation of the process, not a new profile itself.
```

🎯 Target: plan-it — route was explicitly locked in your input ("plan-it."), kept as-is
💡 Disagreement worth flagging: this reads as a single well-scoped deliverable with a DoD you already stated in full — more `fable-it`- or `iterate`-shaped than a fuzzy/large idea needing plan-it's discovery pipeline. Kept your locked route per contract, but if you want the faster path, swap the first line to `/fable-it` and the sketch above already has everything fable-it's DoD slot needs.
