Confirmed: this session (running in the `prompt-it` skill repo) has zero visibility into the actual brain-agent/brain-apps product codebase, so no pointers could be validated here — that grounding is plan-it's own job once it runs from the real repo. Routing to **plan-it**: this is a fuzzy, cross-service capability (tenant-facing MCP install spanning integrations settings UI, brain-agent runtime, and brain-apps) that needs discovery + a spec package, not a locked DoD yet.

```
/plan-it

We support MCP in principle but have no tenant-facing way to install third-party MCP servers (e.g. HubSpot, Airtable — both ship their own MCP servers). Right now MCP capability is only as good as what we've wired by hand; there's no self-serve path for a tenant to add one.

/goal Give tenants a plug-and-play way to install and manage third-party MCP servers, with both brain-agent and brain-apps having runtime visibility into what's installed per tenant — turn MCP from a manual/backend capability into a real tenant-facing integration surface.

Context (from the requester, not yet grounded in the repo — plan-it should verify all of this against the actual codebase in Phase 3/4 discovery):
- Pattern to imitate: the existing Integrations settings page already has sections for "backfill" and "webhooks" — the new MCP section should follow that same settings-page pattern (add-configure-list-remove an integration), not invent a new UI paradigm.
- Two distinct consumers need visibility into a tenant's installed MCP servers: brain-agent (needs to enumerate + invoke a tenant's installed MCP servers' tools at runtime) and brain-apps (needs its own visibility into what's installed — requester did not specify for what purpose; confirm during discovery).
- Reference third parties: HubSpot and Airtable, as examples of vendors that ship ready-made MCP servers — treat as the first real-world integration targets/test cases, not a request to build bespoke MCP servers for them from scratch.
- Credential/secret handling for the new MCP connections should be evaluated against however backfill/webhooks integrations already store tenant secrets today — don't invent a new secret-storage path without checking for an existing one first.

DoD sketch (light — plan-it Phase 1 owns the binding lock):
1. Integrations settings exposes an MCP section where a tenant can add, configure, and remove an MCP server connection.
2. A tenant-added MCP server (e.g. HubSpot or Airtable) is discoverable and invocable by brain-agent at runtime.
3. brain-apps can read/enumerate a tenant's installed MCP servers.
4. MCP connection credentials are stored consistently with the existing backfill/webhooks secret-handling mechanism (or a documented, deliberate deviation from it).

Out of scope / do not touch: building custom/bespoke MCP servers per vendor (HubSpot and Airtable already ship theirs — this is about the install/connect mechanism, not vendor-specific server code); changes to the backfill or webhooks features themselves beyond using their settings-page pattern as a reference.
```

🎯 Target: **plan-it** — this is a fuzzy, multi-service capability (settings UI + brain-agent + brain-apps) that needs discovery and a spec package before there's a buildable DoD, not a single fix-test loop.

💡 I couldn't validate any pointers this turn — the current session runs inside the `prompt-it` skill repo, which has no `brain-agent`/`brain-apps`/integrations-settings code in it. The prompt says so explicitly and defers all grounding to plan-it's own discovery phase once run from the actual product repo; paste it there.
