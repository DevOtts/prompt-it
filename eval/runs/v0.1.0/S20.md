This is a Mode 2 (post-review continuation) — three findings from a QA/review pass on brain-admin-ui, plus a known failed-attempt to avoid repeating. I don't have filesystem access to the brain-admin-ui repo in this session (it's outside prompt-it's working directory), so I can't validate exact file paths — I've named pointers by feature/grep-target instead of guessing paths, and flagged that below.

```
/iterate

Goal: Replace the three hardcoded, tenant.language-dependent UI values in brain-admin-ui
(header title, task-table date format, billing currency symbol) with ONE shared
locale/formatting mechanism — not per-component conditionals.

Context:
- Review findings (evidence, this session):
  1. Header title is hardcoded "Painel" — English-locale tenants see Portuguese.
  2. Tasks table renders dates in hardcoded DD/MM/YYYY regardless of tenant.
  3. Billing widget shows hardcoded currency symbol "R$" regardless of tenant.
- Already tried and reverted: inline ternaries on `tenant.language` at each of the three
  call sites — reverted for being unmaintainable (one-off branching per component, no
  shared source of truth). Do not re-propose per-component ternaries.
- First step: grep the codebase for existing locale infra (`tenant.language`,
  `tenant.locale`, any `react-intl`/`next-intl`/`i18next`/`Intl.NumberFormat`/
  `Intl.DateTimeFormat` usage) before choosing a mechanism — reuse what's already there
  rather than adding a library, if a lightweight shared helper (a locale
  context + `t()` / `formatDate()` / `formatCurrency()` utilities) covers the need.

DoD (each independently testable):
1. Header title renders per tenant.language (e.g. "Dashboard" for an English-locale
   tenant, not "Painel") — verify on the admin header for ≥2 tenant locales.
2. Tasks table date column renders in the tenant's locale date format, not hardcoded
   DD/MM/YYYY — verify on the tasks table for an English-locale tenant.
3. Billing widget currency symbol matches the tenant's locale/currency, not hardcoded
   "R$" — verify on the billing widget for a non-BRL tenant.
4. All three are driven by ONE shared mechanism, not three independent conditionals —
   verify by inspecting the diff: no new `tenant.language === 'pt' ? ... : ...` branches.

Out of scope / do not touch: adding new supported languages/currencies beyond what's
needed to prove the fix; translating other UI strings not named in these findings;
changing the tenant data model.
```

🎯 Target: **iterate** — three related, bounded fixes sharing one root cause; fits a single diagnose→fix→test→verify loop, not a discovery project or overnight run.
💡 Reframed all three as one class (scattered hardcoding vs. no shared locale layer) instead of three point fixes, and hard-fenced against repeating the reverted ternary approach — since I couldn't reach the brain-admin-ui repo here, pointers are named by feature/grep-target rather than file path; the next session should confirm exact locations first.

Setup: run from the brain-admin-ui repo root.
