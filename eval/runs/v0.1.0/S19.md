```
/fable-it

brain-api-core's knowledge-graph (ontology) client currently sources its LLM key only from the environment, unlike the Space-scoped backbone-key pattern used elsewhere in the codebase. This creates inconsistent LLM auth: Spaces with their own backbone key silently fall back to the shared env key instead of using their own.

/goal: Make brain-api-core's ontology client (`GraphService.generateAndRunCypher`) resolve the calling Space's backbone LLM key first, falling back to the env var only when no Space key is configured — mirroring trigger-draft.service.ts's resolution pattern.

Context:
- `@brain-api-core/src/common/graph/graph.service.ts:516-519` — `generateAndRunCypher(naturalQuestion, orgSlug?)`: already receives `orgSlug` but hardcodes `process.env.OPENAI_API_KEY` with no Space lookup. This is the fix target (verified this session).
- `@brain-api-core/src/chat/chat.controller.ts:1093` — calls `generateAndRunCypher(message, orgSlug)`; the natural entry point for verification.
- `trigger-draft.service.ts` (assumed: mirror its per-Space-key-then-env-fallback resolution logic — flag if wrong) — **unvalidated**: searched brain-api-core, brain-connectors, brain-agent, brain-apps, master-admin, brain-admin-ui, processor, and all worktrees under `~/Workspace/Loudr` and found no matching file. Locate it (may live in a repo/branch not checked out locally) before mirroring — if it truly doesn't exist, ask Fernando where it lives rather than guessing at the pattern.

DoD sketch:
1. `generateAndRunCypher` resolves the calling Space's backbone key before falling back to `OPENAI_API_KEY` — verification target: the KG chat endpoint (`chat.controller.ts:1093` path) called for a Space with a configured backbone key.
2. Spaces with no backbone key configured still work via the env fallback — verification target: same endpoint called for a Space without one.
3. The resolution logic reuses/matches trigger-draft.service.ts's mechanism rather than a fresh reimplementation — verification target: trigger-draft.service.ts itself, once located.

Out of scope / do not touch: other env-only OpenAI/Anthropic key call sites in brain-api-core with the same bug shape — `search.service.ts:15`, `common/vector/embeddings.ts`, `chat/llm.service.ts`, `data/services/person-resolver.service.ts`, `data/services/org-resolver.service.ts`, `integrations/integrations.service.ts`. Same class of issue, but not part of this ask.
```

🎯 Target: fable-it — deliverable fix with checkable done-ness ("ship to done"), cross-file verification loop; fable-it's own model-tiers reference (not this prompt) decides team/subagent split and tiering.

💡 Two things worth knowing: (1) I dropped your "use claude teams, lower models for subagents" instruction — that's fable-it Step 3's job natively, prompt-it never emits tiering/delegation notes per its own contract. (2) `trigger-draft.service.ts` doesn't exist anywhere in the `~/Workspace/Loudr` tree I could search (checked all 7 repos + worktrees) — it's flagged unvalidated in the prompt so the fable-it session locates it (or confirms with you) before treating it as the pattern to mirror.
