I'm scoped to this `prompt-it` repo for file access — I can't reach `brain-api-core` from here to validate the actual paths of `trigger-draft.service.ts` or the ontology client. Before I compile the prompt, one grounded question:

**Where does the ontology client actually live in `brain-api-core`** (e.g. `src/ontology/ontology.client.ts` or similar), and can you confirm `trigger-draft.service.ts`'s path (I'm guessing something like `src/triggers/trigger-draft.service.ts`)? If you don't know the exact paths offhand, I'll mark them as assumed-unverified in the prompt and have the target session's first action be to locate and confirm both files before touching anything — safer than me guessing wrong paths into a binding DoD.

Also confirming routing: this reads as a single, well-scoped fix-mirror-verify task (not a big autonomous overnight build), so I'm routing it to **`/iterate`** rather than `/fable-it` — the "split via teams, lower-tier subagents" delegation call is `/iterate`'s or `/fable-it`'s to make internally (I don't emit tiering notes per my own boundary rules), so mentioning "use lower models for subagents" in your ask is actually already satisfied by picking either target skill — they own that decision, not the prompt I hand back. Let me know if you specifically want the full `/fable-it` overnight-run treatment instead of an in-session `/iterate`.
