Confirmed pointer validation: no `tasks-page`, dropdown component, or CDP e2e spec exists in this repo (it's the prompt-it skill-authoring repo, not the app in question) — so no file/path pointers can be fabricated. Routing to `iterate` per the symptom→fix→verify shape.

🎯 Target: **iterate** — single fix-test-verify loop on a specific failing e2e assertion, in-session; no discovery/spec package needed (rules out plan-it/fable-it).

```
/iterate

Goal: get the tasks-page dropdown e2e passing against the real page — root-cause fix, not a workaround. Current failure:

  "ResizeObserver loop limit exceeded" thrown when the dropdown opens, and the
  element detaches from the DOM mid-click inside the CDP e2e suite. Suite is red.

Context:
- Locate the dropdown component backing the tasks-page filter/actions menu and the CDP e2e spec that drives it (grep for "ResizeObserver" console-error handling and for the failing test's click/select step).
- The detach-mid-click symptom plus a ResizeObserver warning is the classic signature of the dropdown re-rendering (position recalculation) while CDP's click is still resolving — treat it as a real race between layout and interaction, not test flakiness.
- Suppressing/ignoring the ResizeObserver console error, or adding sleeps/retries/longer waits around the click to paper over the timing, does not count as a fix — verify the interaction is deterministic, not just less likely to fail.

Definition of Done:
1. The e2e suite runs green against the real page (no skip, no `.only` bypass, no deleted assertions).
2. The dropdown open→select interaction is still genuinely exercised in the test (element resolved live, not stubbed).

Out of scope / do not touch: do not delete, skip, or weaken the failing test to make the suite pass; do not silence the ResizeObserver error via a global error-suppression handler as the fix.
```

💡 No repo pointers exist for this component/test in the current directory, so the prompt directs the session to locate them itself rather than shipping fabricated file paths — and it explicitly forecloses the two known false-green fixes (error suppression, added waits) since "passing" alone doesn't prove the race is gone.
