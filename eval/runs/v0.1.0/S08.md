```
/iterate

Trace-naming work, continuing: chat-origin calls now arrive in Langfuse (project praxya) correctly named "chat" — verified, don't touch that path.

/goal: every LLM call reaches Langfuse with an origin name, regardless of where it was triggered from — not just the chat path.

Context:
- Working reference: whichever wrapper/client call sets `name: "chat"` on the chat origin's Langfuse trace is the pattern to generalize — read it first, don't reinvent the tagging mechanism. (unvalidated — brain-apps and the praxya Langfuse project are outside this session's working tree; target session must confirm paths first)
- Treat the two FAILs below as one gap, not two: origin-name tagging exists on the chat call path and was never propagated to the others. Check whether origin name is a param the chat path happens to pass (and the others omit), vs. something chat's wrapper hardcodes — that distinction determines whether this is a 1-line fix per site or a wrapper change.
- Gap 1: 3 call sites in brain-apps/pulse emit bare "llm.generate" instead of an origin name.
- Gap 2: automation-triggered calls also arrive unnamed — confirm first whether these route through the same wrapper as brain-apps/pulse or a separate trigger path, since that changes whether one fix covers both.

DoD sketch:
1. Each of the 3 brain-apps/pulse call sites shows a named (non-"llm.generate") trace in Langfuse project praxya — verify against that project's trace list filtered to those call sites.
2. An automation-triggered call shows a named trace in the same project — verify the same way, filtered to an automation-triggered run.
3. Chat-origin traces still show "chat" after the change (regression check on the already-passing path).

Out of scope / do not touch:
- The chat call path's existing naming mechanism — extend it, don't rewrite it.
- Call origins not covered by this review (only brain-apps/pulse and automation triggers are confirmed gaps — don't expand the hunt to other origins in this loop).
```

🎯 Target: iterate — bounded fix-verify loop over a known, small set of call sites with a concrete verification target (Langfuse project praxya); no discovery-heavy planning needed.

💡 brain-apps/pulse and the praxya Langfuse project live outside this session's sandboxed working tree, so those pointers are kept but flagged unvalidated rather than dropped; also reframed the two FAILs as one mechanism-level gap (origin-name tagging never propagated past the chat call path) instead of two separate fixes, per the class-over-instance rule.
