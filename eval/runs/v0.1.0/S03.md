```
Route to review-it.

/goal: Independently verify whether the feedback thumbs-up/down component is actually implemented, functional, and live in the test environment — not just marked done in the team's report — before this is represented to the client as finished.

CONTEXT PACKAGE:
- "Last session's report" claiming completion — (assumed: not yet located — searched the current repo (`prompt-it`, which is unrelated tooling, not the client project) and the session-history ledger at `~/Workspace/Vault/OttVault/history_sessions/` for "thumbs" / "feedback" and found no matching session card; flag if wrong). Target session must first locate the actual project repo and the report (check that project's own `.fable-it-reports/`, `.taskstate/`, or its `.agents/history/INDEX.md` ledger for the session that claims this shipped).
- Test env URL where the component is supposedly deployed — (assumed: unspecified — target session must get this from the team or the project's deploy config; flag if wrong).

DoD SKETCH:
1. The thumbs-up/down control renders on the actual page/flow where feedback is expected — target: the live test-env page itself, not a local build.
2. A thumbs-up click registers and persists — target: the backing DB row / API response / state change the report claims exists.
3. A thumbs-down click registers and persists — target: same as above, opposite value.
4. No console errors or broken UI state on interaction — target: browser console + rendered UI in the test env.
5. The report's specific claims (which pages, which states, "deployed to test env") match what is actually observable — target: side-by-side diff of report claim vs. live behavior.

SCOPE FENCES: Out of scope — do not fix any bugs found, do not deploy or touch the test env, do not evaluate visual/UX design quality. This is a verification-only pass to confirm or refute the "finished and deployed" claim before it reaches the client.
```

🎯 Target: review-it — this is a "did the team's completion claim actually hold up" verification ask, which review-it owns (not fable-it/plan-it).
💡 I couldn't validate the core pointer: this working directory (`prompt-it`) and the session-history ledger have no record of a "thumbs up/down feedback component" — that project/session lives elsewhere. I flagged both the report location and the test-env URL as unresolved assumptions inside the prompt rather than blocking on them.

Two things would let me tighten this further — answer either and I'll fold it in:
1. Which repo/project is this component actually in? (so I can validate the report pointer for real instead of flagging it)
2. Do you have the test-env URL, or should review-it be told to discover it from the project's deploy config?
