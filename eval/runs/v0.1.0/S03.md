```
/review-it

/goal Verify the team's claim that the feedback thumbs up/down component is finished and deployed to the test environment — this is a client-facing go/no-go before Fernando reports it as done.

Under test: the completion claim in the last session's report for the component's project — expected at `.fable-it-reports/report.md` or the session card in `.agents/history/` of that repo (unvalidated — target session must locate the report first; the claim is NOT in the current prompt-it repo's reports, already checked). Verify the claim against the DoD/test contract that report was built against, and against the live test-env deployment the report names (assumed: the report states the test-env URL — flag if wrong).

Out of scope / do not touch: no fixes or code changes — verdict and evidence only. Only the thumbs up/down component claim is under review, not other items in the report.
```
🎯 Target: review-it — "is it actually done" verification of an existing completion claim is exactly its Mode; it owns oracle resolution and how-to-verify.
💡 Pointer validation moved the goalposts: the current repo (prompt-it) has a `.fable-it-reports/report.md` but it contains zero mention of the feedback/thumbs component — so "the last session's report" lives in another project's tree, and the prompt marks that pointer unvalidated rather than pretending it's confirmed.

Run this from the repo where the component was actually built, not from prompt-it.
