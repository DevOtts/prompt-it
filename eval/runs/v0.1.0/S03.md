I checked for the "last session's report" both in this repo (`.taskstate/`, `.fable-it-reports/`) and confirmed I can't reach outside `prompt-it` from this sandbox — so I can't validate where that report or the "feedback thumbs up/down" component actually live. Two quick things before I compile the prompt:

1. **Which repo/project** is the feedback component in? (This session is sandboxed to `prompt-it`, which is unrelated.)
2. **Where does "the last session's report" live** — a `/fable-it` or `/review-it` report file, a session-history ledger card (`.agents/history/`), or somewhere else (Slack/Notion)?

Once I know the repo, I'll validate that a report actually exists there before it goes into the prompt — a dead pointer would just burn the next session's first turn chasing it.

For reference, here's the shape the compiled prompt will take (route: **review-it**, since "check it's actually done" is a verification ask, not a build/fix ask):

```
[review-it invocation]

/goal: Verify whether the feedback thumbs up/down component is actually finished and working in <test env>, per the done-claim in <session/report pointer>.

CONTEXT PACKAGE:
- Claim under test: <component name/location>
- Claim source: <report pointer — file:line or session ledger card>
- Where to check: <test env URL/page>

Out of scope / do not touch: this is a verification pass only — do not fix, patch, or redeploy anything found broken; report findings for a follow-up.
```

I'll fill in the pointers for real once you confirm the repo — that's the part actually worth validating before you hand this to a new session.
