This maps to a "review-it — verify a done-claim" ask, but I can't resolve two things from this repo: I'm currently in `prompt-it` (a skill-authoring repo with no feedback/thumbs component), and there's no session-history ledger here (`.agents/history/INDEX.md` doesn't exist in this tree) to locate "the last session's report."

Two quick questions before I compile the prompt:

1. **Which repo/project** is the feedback thumbs component actually in? (Not this one — `prompt-it` has no such feature.)
2. **Where does "the last session's report" live** — a specific file path, a `/read-chat "<session-alias>"` reference, or do you want to paste the relevant claim text here?

Once I have either of those, I'll validate the pointer and compile the review-it prompt (claim-under-test + where the claim lives, review-only fences — no fresh DoD, no verification-method prescriptions, since that's review-it's job to design).
