```
/review-it

The team reports the feedback thumbs-up/down component is finished and deployed to the test env — that claim is recorded in the last session's report (assumed: `.agents/history/INDEX.md` card or `/read-chat`-able session in [PROJECT_REPO] — flag if wrong, not found in the current working tree). Before telling the client it's done, verify the claim rather than taking it at face value.

/goal Verify the feedback thumbs-up/down component is actually complete and working in the test environment, exactly as claimed in the last session's report — confirm or refute before the client is told.

CONTEXT PACKAGE
- Last session's report (assumed: located via `/read-chat "<session alias>"` or the project's `.agents/history/` ledger — flag if wrong) — source of the "finished + deployed" claim and its stated DoD/acceptance criteria
- Test env URL/deployment (assumed: named in that same report — flag if wrong) — where the live check must run
- [PROJECT_REPO] (unvalidated — target session must confirm this path first) — this check could not be run from the prompt-it repo, which has no feedback component

DoD SKETCH
1. Pull the exact claim and acceptance criteria the last session's report states for this component — verify against those, not a new checklist
2. Confirm the component is present and functional in the test env deployment itself (not just in code/PR)
3. Confirm each criterion the report claims as "done" independently, citing what was actually observed vs. what was claimed

Out of scope / do not touch: do not fix any bugs found — report status only; do not verify unrelated components; do not message the client — that decision stays with the user pending this verification.
```

🎯 Target: review-it — this is a "is it actually done" verification ask, checking a specific claim before an external commitment (telling the client), which is exactly review-it's job, not a fresh build task.

💡 I couldn't validate any pointers — this working directory (prompt-it) has no feedback/thumbs-up-down component and no session ledger entry for it, so the report location, test env URL, and target repo are all flagged assumptions rather than confirmed pointers.

Which project/repo is this in, and do you know the session name/alias for "the last session's report" (so it can be pulled via `/read-chat`) or its file path? Also, what's the test env URL?
