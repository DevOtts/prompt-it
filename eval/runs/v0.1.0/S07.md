This session is sandboxed to `prompt-it`, so I can't reach the praxya repo to validate file-level pointers (component paths, the intel-generation service file) — same limit the S03 eval fixture hit. I've kept the prompt at the mechanism level rather than guessing paths.

Routed to **iterate**: both gaps are bounded, single-session fix-test-verify loops with a clear reference implementation (chat page) to extend — not a multi-epic build.

```
/iterate — observability epic: close the two review FAILs below.

/goal: Extend the feedback thumbs UI to the Intel and Design pages, and split the flat multi-step intel-generation trace into per-step spans in Langfuse — without touching what's already verified working.

CONTEXT PACKAGE:
- Verified working, do not re-check or modify: Langfuse deployed globally, per-tenant projects live, chat traces arriving — confirmed in Langfuse UI, project `praxya`.
- Gap 1 evidence: the feedback thumbs component renders only on the chat page; the Intel and Design pages have LLM interactions with no feedback UI at all.
- Gap 2 evidence: multi-step intel generation shows up as a single flat `llm.generate` trace in Langfuse — no per-step breakdown, so cost per step can't be attributed.
- Pattern that already works: the chat page's feedback thumbs component + its trace/response linkage is the reference implementation for gap 1 — mount that same mechanism against Intel's and Design's LLM response render points rather than rebuilding it.

DoD SKETCH:
1. Intel page: triggering an LLM interaction renders the feedback thumbs UI. Verification target: Intel page, staging/dev.
2. Design page: triggering an LLM interaction renders the feedback thumbs UI. Verification target: Design page, staging/dev.
3. A fresh intel-generation run produces a Langfuse trace with separate per-step spans/generations (not one flat `llm.generate`), each attributable to its own cost. Verification target: Langfuse UI, project `praxya`, trace for the new run.

Out of scope / do not touch:
- Langfuse deployment and per-tenant project config — already verified working.
- The chat page's feedback thumbs component and its trace wiring — extend/reuse it, don't rebuild it.
```

🎯 Target: iterate — two bounded, independently testable fixes with an existing reference implementation to extend, not a fresh build.
💡 I couldn't validate any file paths (component location, intel-generation service file) — this sandbox has no access to the praxya repo — so the prompt names surfaces and mechanisms, not files; the iterate session will locate the actual files itself.
