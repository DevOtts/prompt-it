Before drafting the prompt, I need to flag something important I found in the repo — the ask as stated conflicts with a decision Beacon already made and shipped.

**What's already done** (not gaps):
- `FeedbackWidget` (thumbs up/down) is already mounted on both `Intel.tsx` (line 476) and `DesignChat.tsx` (line 1441, 2095), keyed on `trace_id`.
- The "what went wrong" form already exists and is already wired in: `FeedbackWidget.tsx:74-92` opens `FeedbackReasonForm` automatically on thumbs-down. This is live on both pages today.

**The actual gap and the conflict:**
- "Pop a review modal after each generation" is the exact pattern Beacon removed on 2026-07-16 (`docs/release-notes/2026-07-16-sow-feedback-ux.md`, ADR-041 in `docs/ai-context/decisions.md:713`). A blocking post-generation popup on SoW got a user complaint (blocked reading the doc it was asking about — "chicken-and-egg"). It was replaced with the non-blocking inline thumbs + reason form — the exact thing already on Intel/Design.
- What SoW/Brief *do* have that Intel/Design don't: a pre-regenerate intercept modal (`RegenerateFeedbackModal.tsx`, ADR-041) that pops **before** a regenerate action fires, pre-set to thumbs-down. `Intel.tsx` has no regenerate action (single-shot query). `DesignChat.tsx` has one (`handleRegenerateLast`, line 468) but no intercept modal on it yet.

Two questions before I compile the prompt:

1. Do you actually want a **blocking modal on every generation** for Intel/Design (reopening the pattern just rejected for SoW), or did you mean the **regenerate-intercept modal** pattern (pop before regenerating, not after generating) extended to Design chat's regenerate action — consistent with ADR-041?
2. Is "all the regenerations" scoped to Design chat's `handleRegenerateLast` only (Intel has none), or is there another regeneration path elsewhere in Beacon I should check?
