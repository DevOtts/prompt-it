```
/plan-it

Beacon (/Users/macbook/Workspace/ANM/Beacon) has feedback-UX work in flight across two epics that this
request lands directly on top of: Epic #59 "Feedback & Observability" (E3 widget-coverage + E4
review-modal/reason-form, both still Backlog per the tracker) and Epic #69 "SOW Feedback UX Redesign"
(done 2026-07-16, archived to 0-done). Epic #69 explicitly REMOVED a post-generation popup modal from
the SOW page — Fernando's own locked decision D1 — because it blocked reading the content it was asking
about, and its audit (D8) concluded Intel/Design/Brief need "no action" on that pattern. Any plan here
must reconcile with that ruling, not silently reintroduce the pattern it reversed.

/goal Give every Beacon surface where a user reads LLM-generated output (Intel, Design chat, and all
regeneration flows) thumbs feedback capture plus a lightweight "what went wrong" detail form on
thumbs-down — reusing the existing feedback pipeline and components rather than rebuilding them, and
resolving how (or whether) a post-generation review moment applies to Intel/Design given the SOW
reversal.

CONTEXT PACKAGE
- `beacon/docs/implementation/0-done/3-sow-feedback-ux/PRD-sow-feedback-ux.md` — read FIRST: locked
  decisions D1 (kill popup-on-complete) + D8 (Intel/Design/Brief audited, no blocker pattern found).
- `beacon/docs/implementation/2-feedback-observability/epics/epics-feedback-obs.md` — E3 (design-chat
  trace+widget coverage) + E4 (review modal & reason form) test-contract spec, still Backlog.
- `beacon/docs/implementation/2-feedback-observability/02-design-and-contract.md` — frozen CONTRACT E3/E4
  were speced against; DEC-1..DEC-9 in `.taskstate/feedback-obs-decisions.md` (repo root) record build-time
  reconciliations from a prior fable-it run on this same epic (rating encoding, trace_id shape, modal cooldown storage).
- `beacon/frontend/src/components/FeedbackWidget.tsx`, `FeedbackReasonForm.tsx`, `SowFeedbackCard.tsx`,
  `RegenerateFeedbackModal.tsx` — already-built, reusable pieces (FeedbackReasonForm IS the dimensions+comment
  "what went wrong" form D2/D4 already ship for SoW) — extend these, don't recreate.
- `beacon/frontend/src/pages/Intel.tsx:476` and `DesignChat.tsx:1441,2095` — FeedbackWidget is ALREADY
  mounted per-message on both surfaces, keyed on `trace_id` (assumed: thumbs coverage on Intel/Design may
  already be largely done and the real gap is the reason-form + regeneration flows — flag if wrong).
- `.claude/pm/tracker.md` — Epic #59/#69 rows, GH-Projects-cached current status.

DoD SKETCH (light — plan-it locks the real one)
1. Every page where a user reads an LLM generation (Intel, Design chat, each regenerate path) has thumbs
   capture keyed on a real trace_id — target: a `llm_feedback` row per surface, correctly keyed.
2. Thumbs-down opens a reason-detail form (dimensions + free comment) — target: the row's `dimensions`
   JSONB + `comment` populate on submit.
3. The post-generation review moment for Intel/Design follows whatever pattern this plan locks (blocking
   popup only if explicitly re-decided; otherwise the SOW-reversal precedent — non-blocking card/widget +
   pre-action intercept) — target: the plan's decision gate records which, applied consistently.

SCOPE FENCES — Out of scope / do not touch
- SoW/Brief feedback UX (Epic #69) is shipped and done — do not reopen or re-litigate it.
- Langfuse score-push, trace-naming/nesting internals (Epic #59 E1/E2/E5) — untouched unless a surfaced
  gap forces it.
```

🎯 Target: **plan-it** — multi-surface feature cluster (Intel + Design + all regenerations) with genuinely unresolved scope: it collides with a locked, shipped decision (Epic #69, 2026-07-16) that reversed the exact "popup after generation" pattern being requested here.

💡 Biggest catch: Beacon already has a done epic that killed the post-generation-popup pattern site-wide (chicken-and-egg — you can't read the SoW while the modal blocks it) and audited Intel/Design as needing no such modal. Your ask risks re-shipping something Fernando already reversed a week ago — I routed to plan-it specifically so its decision gate resolves that on purpose instead of by accident. Also: thumbs widgets and a reusable reason-form component already exist on Intel/DesignChat — likely extend, not build from scratch.

Two questions worth answering before the plan-it session starts (optional — the prompt above works without them, assumptions are flagged inline):
1. For Intel/Design, do you actually want a blocking popup after every generation (reversing the SOW precedent), or the same non-blocking pattern (bottom card/discreet widget + pre-regenerate intercept) SOW just shipped?
2. Does "all the regenerations" include Brief's regenerate flow, or just Intel/Design?
