```
/fable-it

Goal: Give Beacon's Design-chat regenerate actions the same review-modal + thumbs-down
reason-form coverage that Statement of Work and Brief already have, closing the one
remaining gap in the feedback-observability initiative's E4 epic.

Grounding: Per-turn thumbs (FeedbackWidget) already cover every LLM chat surface —
Intel (Intel.tsx:476), Design chat (DesignChat.tsx:2096, keyed on message.trace_id),
SOW (SowEditor.tsx) and Brief. The regenerate-intercept review modal (RegenerateFeedbackModal
+ FeedbackReasonForm, default thumbs-down, "what went wrong" dimensions+comment) shipped for
SOW/Brief via docs/implementation/0-done/3-sow-feedback-ux/ but was never wired into Design
chat's two regenerate paths. This is the open half of epic E4 in
docs/implementation/2-feedback-observability/epics/epics-feedback-obs.md (test cases #15-18).

Context package:
- frontend/src/components/RegenerateFeedbackModal.tsx + FeedbackReasonForm.tsx — existing,
  reusable components; do not fork them, wire Design chat into the same components.
- frontend/src/pages/SowEditor.tsx:1164 and frontend/src/components/sow/BriefEditor.tsx —
  live reference wiring to mirror (pattern-to-imitate).
- frontend/src/pages/DesignChat.tsx:468 (handleRegenerateLast, sync call, no Celery task_id)
  — mirror the FeedbackWidget mount at DesignChat.tsx:2096-97, which keys generationTaskId
  on the last assistant message's trace_id; use the same id for the modal.
- frontend/src/pages/DesignChat.tsx:1029 ("Regenerate artifacts" button) and its
  poll-terminal completion handler (~DesignChat.tsx:627, referenced as :622 in
  docs/implementation/2-feedback-observability/KICKOFF.md) — key on session.artifacts_task_id,
  same id the artifacts-pane FeedbackWidget already uses.
- docs/implementation/2-feedback-observability/epics/epics-feedback-obs.md E4 — binding
  test contract (cases #15-18) to extend onto the design_chat/design_artifacts features.
- docs/implementation/0-done/3-sow-feedback-ux/PRD-sow-feedback-ux.md §4 (G-rules v1.0) —
  the locked conventions the SOW/Brief build followed (rating 1=up/0=down, session.id
  untouched, feedback writes never block on Langfuse) — Design chat's wiring must match, not
  reinterpret them.

DoD sketch (verification targets, not methods):
1. "Regenerate last answer" in Design chat is gated by the review modal with default
   thumbs-down on skip — target: E4 test case #18 (down-vote → reason form → submit) and
   #17 (fires once, respects cooldown/opt-out), run against the Design-chat regenerate flow.
2. "Regenerate artifacts" and the artifacts-generation-complete handler carry the same
   modal gating — target: E4 test cases #15 and #17 applied to the design_artifacts path.
3. Thumbs-down reason-form submissions from Design chat persist dimensions + comment on the
   llm_feedback row exactly as SOW/Brief do today — target: E4 test case #16, feature=
   design_chat/design_artifacts.
4. Existing thumbs coverage on Intel, Design (per-message), SOW, and Brief is unbroken —
   target: E3 test case #14 (regression) plus a manual check each FeedbackWidget mount still
   posts.

Scope fences — do not touch:
- Intel.tsx: confirmed no regenerate/overwrite action exists there (only an internal
  session-retry at Intel.tsx:223, not user-facing) — thumbs coverage is already correct and
  out of scope (assumed: no hidden regenerate path was missed — flag if wrong).
- SowEditor.tsx / BriefEditor.tsx wiring: already shipped, touch only if a regression surfaces.
- Epics E1 (span-naming canon), E2 (nested spans), E5 (Langfuse score push) in
  2-feedback-observability: unrelated to this ask, do not pull into scope.
- Do not author a new test contract — extend/reuse E4's existing cases #15-18 for the Design
  surface.
```

🎯 Target: fable-it — bounded, single-surface (Design chat) extension of a shipped, provable pattern with an existing binding test contract; not the fuzzy multi-surface build the rough ask implied.

💡 Your framing ("extend thumbs to every page") was already true — thumbs exist on Intel, Design, SOW, and Brief today. The actual gap, confirmed by reading the code, is narrower: only Design chat's two regenerate actions lack the review-modal + reason-form pattern that SOW/Brief already shipped (epics-feedback-obs.md E4, cases #15-18) — Intel has no regenerate action at all, so it needs nothing.
