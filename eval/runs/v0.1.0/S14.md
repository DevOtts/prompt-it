Grounding complete. This maps directly onto an existing, already-authored epic in the repo (epic #59, E3+E4) with a frozen PRD/contract and test cases — and current code shows part of it may already be shipped. Also found a real precedent conflict worth flagging rather than silently resolving. Here's the compiled prompt.

```
/fable-it

GROUNDING: Beacon already has a frozen PRD + CONTRACT + 20-case Test Contract for exactly
this feature — epic #59 "Feedback & Observability Enhancement", specifically E3 (issue #62,
Design-chat trace + widget coverage) and E4 (issue #63, review modal + thumbs-down reason
form). Docs: `docs/implementation/2-feedback-observability/{prds/PRD-master.md,
02-design-and-contract.md, epics/epics-feedback-obs.md}` (repo root note below). A separate,
more recent epic (#69 "SOW Feedback UX", shipped 2026-07-16, see `.taskstate/sow-fb-ux-*.md`)
already built the SoW/Brief version of this same feature — reuse its components, but do not
touch its surfaces.

/goal: Extend the FeedbackWidget thumbs to every remaining LLM-chat page (Intel, Design,
and all regenerate flows), add a post-generation review modal, and add a thumbs-down
"what went wrong" reason form — using E3+E4's existing frozen contract as the DoD.

CONTEXT PACKAGE (repo root is the nested `beacon/` app dir — confirmed via docs/frontend
paths: `/Users/macbook/Workspace/ANM/Beacon/beacon/`, not the outer workspace folder):
- `docs/implementation/2-feedback-observability/epics/epics-feedback-obs.md` §E3/§E4 — the
  authored 8-case Test Contract (cases #11–18) for this exact ask; §E4 also lists the current
  task checklist (reason-form component, modal, 24h/don't-ask-again cap storage).
- `docs/implementation/2-feedback-observability/02-design-and-contract.md` — frozen CONTRACT
  E3/E4 are scoped against.
- `.taskstate/decisions.md` (DEC/D4-D6) + `.taskstate/feedback-obs-decisions.md` (DEC-1..9) —
  binding shared conventions: rating encoding `1=up/0=down`, trace_id keying per surface,
  feature-string naming, feedback-write-never-blocks-on-Langfuse invariant.
- Pre-ground fresh against current `main` before planning work — do not trust the docs'
  "Backlog" status as-is (repo's own tracker.md flags itself as a stale cache). Grep this
  session already found `FeedbackWidget` mounted and trace_id-keyed in BOTH
  `frontend/src/pages/Intel.tsx` (~:474, keyed on `msg.trace_id`) and
  `frontend/src/pages/DesignChat.tsx` (~:1439, keyed on `session.artifacts_task_id`) — E3's
  widget-coverage may already be shipped; verify per-surface against the Test Contract rather
  than re-building blind.
- Reusable components already in the repo — mirror/extend rather than rebuild:
  `frontend/src/components/FeedbackReasonForm.tsx`, `RegenerateFeedbackModal.tsx`,
  `SowFeedbackCard.tsx`, `FeedbackWidget.tsx`.
- OPEN DESIGN DECISION to reconcile before building E4's modal (do not silently pick either
  side): the literal ask is "pop a modal after each generation," which is also E4's original
  spec (Test #17: "modal fires once on completion"). But epic #69 tried exactly that pattern
  for SoW/Brief and Fernando's own staging validation reversed it (`.taskstate/
  sow-fb-ux-decisions.md` RD-5, RD-8) in favor of an always-visible feedback card +
  regenerate-only intercept modal (no popup on every plain completion). Pre-ground both,
  record the choice as a DEC entry the same way RD-4/RD-8 did, and flag it in the run report
  for Fernando to confirm on staging before/at the prod gate — same validation loop already
  used for RD-8.

DoD SKETCH (verification targets — the existing Test Contract IS the binding DoD, do not
re-author it):
1. Thumbs widget present, trace_id-keyed, and functional on every LLM-chat surface — verify
   against Test Contract #11–14 (Intel + Design chat + regenerate mints distinct trace_ids +
   widget never posts without a trace id).
2. Post-generation review modal — verify against Test Contract #17, resolved per the open
   design decision above (cooldown/opt-out storage per #15).
3. Thumbs-down "what went wrong" form — verify against Test Contract #16 + #18 (dimensions +
   comment persisted via upsert, full down-vote → form → submit flow in-browser via CDP).

SCOPE FENCES — Out of scope / do not touch:
- E1 (#60, trace-naming canon), E2 (#61, nested spans), E5 (#64, Langfuse score push) — these
  are observability infra, not part of this UI ask. Only touch them if a genuine E3/E4
  prerequisite gap blocks the build (verify first — DEC-2/DEC-3 suggest trace threading may
  already be done from the 2026-07-02 observability run).
- Do not modify or revert any SoW/Brief feedback surface shipped by epic #69 (`SowEditor.tsx`,
  `BriefEditor.tsx`, the RD-5/RD-8 card+intercept pattern there) — that work is done, merged,
  and Fernando-validated; treat it as reference only.
```

🎯 Target: fable-it — deliverable goal with a checkable DoD, and the DoD already exists as an authored, frozen test contract in the repo (not something to spec from scratch).

💡 The literal ask ("modal after each generation") is the same pattern epic #69 already tried and reversed for SoW after Fernando's staging feedback — I kept your wording as the ask but flagged the conflict explicitly rather than silently building either version, and pointed at code (`Intel.tsx`, `DesignChat.tsx`) showing the thumbs widget may already be mounted on 2 of your 3 target pages.
