````
/plan-it

Beacon already has a full LLM-feedback loop on the SOW surface: thumbs on generated
output, a regenerate modal, a reason form, and a backend feedback route wired into
Langfuse scores and nightly curation. The other LLM chat surfaces ship with none of it.

/goal Every surface where users chat with or regenerate from the LLM captures the same
structured feedback as SOW — thumbs on each generation, a review modal popping after each
generation completes, and a short "what went wrong" form on thumbs-down — so the Langfuse
scoring + nightly-curation pipeline sees all surfaces, not just SOW.

Context package (mirror the SOW implementation — it already solves component + API +
Langfuse wiring; generalize, don't duplicate):
- beacon/frontend/src/components/SowFeedbackCard.tsx — the thumbs pattern to generalize
- beacon/frontend/src/components/RegenerateFeedbackModal.tsx and FeedbackReasonForm.tsx —
  modal + reason form already exist; reuse/generalize these for the post-generation review
  modal and the thumbs-down form (assumed: reuse these rather than build new components —
  flag if you want a different UX)
- beacon/backend/api/routes/feedback.py — the feedback API; check whether it already
  accepts non-SOW target types before adding endpoints
- beacon/frontend/src/pages/Intel.tsx and DesignChat.tsx — the two named target surfaces
  (assumed: "all the regenerations" = every regenerate action on every surface, including
  existing SOW section regens — flag if wrong)
- .taskstate/sow-fb-ux-run-state.md and .taskstate/feedback-obs-grounding.md — prior
  feedback-UX sessions' state and decisions; read these before re-deciding anything

DoD sketch:
1. Thumbs up/down render on every LLM generation on the Intel and Design chat pages, and
   a click persists via the backend feedback route (visible as a row reachable by that
   route's store).
2. Every regeneration flow, on every surface, carries the same feedback affordance.
3. A review modal appears after each generation completes; submitting it lands a score
   visible in the Langfuse scores path.
4. Thumbs-down opens the "what went wrong" form; its reason text reaches the feedback
   route and is picked up by the nightly-curation input.
5. Existing SOW feedback behavior is unchanged (its current tests still pass).

Out of scope / do not touch: nightly-curation logic itself, Langfuse scoring internals,
prompt content, and generation quality — this extends the capture surface only.
````

🎯 Target: plan-it — multi-surface feature cluster (two pages + all regeneration flows + new modal trigger) with unresolved scope edges; pointers are good but scope isn't locked, so it needs the planning pass before a build run.

💡 Validated the repo and found the modal and reason form **already exist** (`RegenerateFeedbackModal.tsx`, `FeedbackReasonForm.tsx`) plus prior feedback-UX session state in `.taskstate/` — reframed the ask from greenfield to generalization and pointed the planner at the prior decisions so it doesn't re-litigate them.

Run from `/Users/macbook/Workspace/ANM/Beacon`.

Optional refinements (answer and I'll tighten):
1. "Review modal after each generation" — auto-pop the existing RegenerateFeedbackModal-style dialog, or a new lighter rating-only modal?
2. Does "all the regenerations" include SOW section regens (which may already have feedback), or only the currently-uncovered surfaces?
