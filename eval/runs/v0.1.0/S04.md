Confirmed: this repo (`prompt-it`) is a markdown/skill-authoring plugin repo — no `package.json`, no `pnpm-lock.yaml`, no TypeScript files anywhere, and no `src/eval/runner.ts` exists. `eval/scripts/run-eval.sh` is a shell script, not TS. The pointer you gave doesn't resolve here — this error must belong to a different project.

I can't compile a grounded prompt against a pointer that doesn't exist in the current working directory; a prompt with a dead pointer is worse than no prompt.

**Before I draft anything, which repo is this actually in?** Options:
1. A different local project you meant to `cd` into (tell me the path)
2. This repo, but the file hasn't been created yet as part of in-progress eval-harness work (check `.taskstate/` / `eval/scripts/`?)

Let me know the right working directory (or confirm you want me to search elsewhere on disk) and I'll validate the pointer and produce the routed fix-it prompt.
