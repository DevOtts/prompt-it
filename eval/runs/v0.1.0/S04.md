```
/iterate
Fix the TypeScript build error so `pnpm build` passes clean.

Error:
`src/eval/runner.ts(42,18): error TS2345: Argument of type 'string | undefined' is not assignable to parameter of type 'string'.`

(assumed: this is a different repo, not prompt-it — `src/eval/runner.ts` doesn't exist in the prompt-it working tree this prompt was compiled in; confirm you're in the correct repo before starting — flag if wrong)

Context: look at src/eval/runner.ts:42 and the function/call whose argument is typed `string | undefined` — trace where that value originates (likely a `.env` lookup, optional field, or `Array.find`/`.get()` result) and fix the type at the source (narrow, default, or guard) rather than casting.

DoD:
1. `pnpm build` exits 0 with no TypeScript errors.
2. No new `any` casts or `// @ts-ignore` introduced to silence the error.

Out of scope: do not touch unrelated type errors or files outside src/eval/ unless the fix requires changing a shared type/interface they depend on.
```

🎯 Target: iterate — single, well-scoped fix-test-verify loop (one TS error, one build command), not a discovery or multi-day task.
💡 The file `src/eval/runner.ts` doesn't exist in this working tree (prompt-it has no `src/` at all) — flagged as an inline assumption since this is a cross-tree pointer I can't validate from here; confirm the target repo before running.
