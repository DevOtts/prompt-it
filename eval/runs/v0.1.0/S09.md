```
/fable-it

A rough review of the Airtable task-write feature found both of its claimed-passing checks were false greens: CDP e2e reported PASS on the tasks dropdown/save-to-tasks flow, but a human running the same flow hits UI errors; and the write-to-Airtable path was marked working without anyone checking Airtable itself.

/goal Make the Airtable task-write feature actually work and prove it with evidence a human — and Airtable itself — would agree with, not evidence from the suite that already lied once.

Context from the review (no repo access in this session — validate against the real code before acting):
- Gap 1: CDP e2e claimed green on the tasks dropdown / save-to-tasks flow. A human running the same flow gets UI errors on the dropdown. The suite is not measuring the failure a user actually hits.
- Gap 2: saving to tasks requires enabling the integration first; once a save reports success in the app, nobody checked Airtable itself — verification stopped at the app's own success state, not the record landing in the third-party system of record.
- Failed attempt (do not repeat): re-running the same CDP suite with longer waits produced the identical false green. The suite's blindness to the dropdown error is structural, not a timing flake — a longer wait will not fix it.

DoD sketch (verification targets, not protocol):
1. Tasks dropdown / save-to-tasks flow completes with zero UI errors — verification target: a human-observed (or human-equivalent, non-CDP-suite) run of the dropdown interaction, since that suite already reported green on this exact broken flow.
2. A task saved through the write flow (integration enabled) is confirmed present in Airtable — verification target: the record as read back from Airtable itself (its API or UI), not the app's own success/toast state.

Out of scope / do not touch: do not "fix" this by re-running the existing CDP suite with longer timeouts/waits — already tried, same false green. Do not mark item 2 done from the app's own success response; the target is Airtable's data, not our UI's claim about it.
```

🎯 Target: **fable-it** — two related but distinct false-green gaps (a blind e2e suite, an unverified external write) need a DoD run with real, honest verification targets, not a single fix-test cycle, so the same false-green pattern can't slip through a second time.

💡 This session has no access to the actual product repo (it's running from `prompt-it`'s own repo), so no file/test pointers could be validated — the prompt discloses that and leaves grounding to fable-it's own discovery. It also acknowledges nothing as "working": both review findings were FAILs with no passing evidence given, so crediting anything here would itself be a false green — the prompt states the gaps directly instead.
