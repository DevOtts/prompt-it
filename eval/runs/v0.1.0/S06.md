Verified: `qa/dryrun-mode2.md` (same repo) documents this exact case with a prior failed-attempt note and a third colliding sense that the rough ask doesn't mention — folding both in below.

```
## Term disambiguation: "cliente" / "client"

Before answering any question containing "cliente(s)" or "client(s)", identify which entity the question refers to — this term collides across multiple unrelated data sources:

1. **Shopify repeat buyer** ("clientes recorrentes", "compradores recorrentes") — a shopper with 2+ orders in the relevant period. Source: Shopify/orders data, not the orgs table.
2. **Org of type=client** — a B2B organization record with type="client" (CRM/orgs table). Source: orgs table filtered by type=client.
3. (assumed: a third sense — the campaign-creator/Meta-ads "client" config, a distinct entity from both above — flag if this doesn't exist in your data model; drop this row if not applicable.)

Resolution rules:
- Default to sense 1 (Shopify repeat buyer) when the question pairs "cliente(s)" with a time period or "recorrente(s)" and names no org/CRM qualifier — this is the common owner-facing intent.
- Default to sense 2 (org type=client) when the question says "org(s)", "conta(s) CRM", or names a specific org explicitly.
- If genuinely ambiguous, state the interpretation used and offer the alternative rather than guessing silently, e.g.: "Interpretei 'clientes' como compradores recorrentes do Shopify (2+ pedidos). Nenhum registro encontrado neste mês. Quer que eu busque contas do tipo cliente (CRM) em vez disso?"
- Never return a bare "no clients found." Name the interpretation that returned zero results: "Nenhum comprador recorrente do Shopify encontrado neste mês" (or the equivalent for whichever sense was queried).

Worked examples:
| Question | Correct interpretation | Behavior |
|---|---|---|
| "Quantos clientes recorrentes tivemos este mês?" | Shopify repeat buyer | Query Shopify orders; count distinct buyers with 2+ orders this month. Never query org type=client for this question. |
| "Quais os clientes configurados para a org bosslife?" | Org type=client | Query orgs table for type=client under the named org. Never query Shopify buyers for this question. |
| "Liste os clientes do bosslife" (no further qualifier) | Ambiguous | Default to org type=client (org named explicitly), then offer: "Quer também os compradores recorrentes do Shopify desta org?" |
```

🎯 Target: product-LLM system prompt (tenant chat agent) — the deliverable is a self-contained instruction fragment to paste directly into the agent's config; no *-it harness applies since this fixes a live product LLM's behavior, not a codebase.

💡 A same-repo lookup (`qa/dryrun-mode2.md`) surfaced context your rough ask didn't mention: this exact "cliente" ambiguity was hit before with a fix already tried and rejected (`"clients = orgs of type=client"` — broke the Shopify-buyer and Meta-ads senses), and a third colliding sense (campaign-creator client config). The rejected fix isn't repeated here since the new fragment is context-sensitive rather than a blanket rule; the third sense is included as a flagged/droppable row rather than asserted as fact, since I can't verify it against your actual data model from this repo.

Paste into the tenant chat agent's system prompt (or its context-injection file, e.g. a SOUL.md/client-bible pattern if your stack uses one) near existing data-access instructions — confirm/drop the flagged third row first.
