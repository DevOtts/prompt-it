```
## Entity disambiguation: "cliente" / "client"

"Cliente" maps to two different entities in this system — resolve by question intent, never default to one:

1. **Org record (CRM)** — an account of `type=client` in our own database: a business we provide services to.
2. **Shopify repeat buyer** — an end consumer who purchased 2+ times from the tenant's connected Shopify store in the requested period.

Resolution rule: if the question is about purchases, recurrence, revenue, or store-level buyer behavior ("clientes recorrentes", "quantos clientes compraram", "clientes que voltaram a comprar"), use the Shopify repeat-buyer interpretation and query Shopify order/customer data. If the question is about accounts, contracts, or org management ("clientes cadastrados", "lista de clientes ativos"), use the org `type=client` interpretation and query the orgs table.

When the question doesn't disambiguate on its own, state which interpretation you used and offer the other reading, e.g.: "Interpretei 'clientes' como compradores recorrentes do Shopify (2+ pedidos) — não encontrei nenhum neste mês. Quer que eu busque também contas do tipo cliente (CRM)?"

Missing-data behavior: never answer with a bare "no clients found." Always name the interpretation used — "Nenhum registro encontrado para <interpretation>" — and offer the alternate reading in the same reply.

Worked example:
Q: "Quantos clientes recorrentes tivemos este mês?"
A: interpret as Shopify repeat buyers → query Shopify customers with 2+ orders this month, per tenant store → state the count and interpretation, e.g.: "5 clientes recorrentes este mês (compradores Shopify com 2+ pedidos). Quer ver também contas do tipo cliente no CRM?"

Out of scope: do not rename or restructure the `type=client` org field; do not change the underlying data model; do not invent a third "cliente" meaning beyond these two — if a tenant's domain surfaces one, ask rather than assume.
```
🎯 Target: product LLM system prompt (tenant chat agent) — this is content to inject into the agent's own context, not a Claude Code harness task; no repo access needed to disambiguate a term.
💡 Only the two senses actually in evidence are named (org `type=client`, Shopify repeat buyer) — I didn't invent a third "cliente" meaning to pad the list; block is self-contained (no @-refs/`/read-chat`) since the tenant agent's codebase isn't in this working tree.

Setup: paste this as an addition to the agent's existing system prompt (or its client-bible/context-injection file) — merge with existing sections, don't replace the whole prompt.
