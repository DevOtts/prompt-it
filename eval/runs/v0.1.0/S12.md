Two issues surfaced during validation: **`docs/architecture/overview.md` doesn't exist** (no `docs/` directory anywhere in this repo, confirmed via `git log --all`), and this repo already has an architecture write-up living at `README.md:254` ("Architecture / What's inside"). So "update" needs to become "create," and the new doc needs to coexist with the existing one rather than duplicate it. I also pulled the actual eval/ contents so the prompt carries real structure, not a guess.

This is a scoped, single-session documentation task — no discovery, no autonomous run, no verification-of-a-claim — so it routes to a **bare session**, not one of the harness skills.

```
Create `docs/architecture/overview.md` in this repo (the `prompt-it` plugin repo) to document the eval suite under `eval/`. The `docs/` directory doesn't exist yet — this is a new file, not an edit.

/goal: give a reader a working mental model of how the eval suite proves prompt-it's output quality — what each piece is for and how a run is produced — without re-deriving it from the files themselves.

Context package (read in this order):
- `eval/sample-prompts.md` — 20 rough input prompts + a coverage matrix (route type, mode, edge cases like dead pointers / legitimate ambiguity / tiering-stripping)
- `eval/expected-prompts.md` — paired oracle: reference exemplar + binding MUST/MUST-NOT checklist per sample (G1–G6 global checks apply to every sample)
- `eval/scripts/run-eval.sh` — headless runner: pipes one sample into a fresh `claude -p --model sonnet` session with the installed plugin, writes `<SID>.md`/`.err` to an output dir
- `eval/runs/v0.1.0/` — actual run output (S01–S12+, `.md` transcripts + `.err` logs) for the v0.1.0 suite
- `eval/fixtures/notes.md` — a fixture referenced by one of the samples
- `README.md:254` "Architecture / What's inside" — the existing architecture section; the new doc must not duplicate its repo-tree listing, only add the eval-suite section that section doesn't cover

DoD sketch:
1. `docs/architecture/overview.md` exists and explains the eval suite's purpose (proving prompt-it's output against a binding oracle) — verification target: file present, reads coherently standalone
2. Documents each of the 5 pieces above (sample-prompts, expected-prompts, run-eval.sh, runs/<version>/, fixtures/) and how they relate — verification target: every `eval/` path named in the doc actually exists (`ls`/`find` each one)
3. Cross-references (not duplicates) `README.md`'s existing Architecture section — verification target: no repo-tree diagram repeated verbatim in both files

Assumption (flag if wrong): the new doc supplements README.md's Architecture section rather than replacing it — if you intended to consolidate both into `docs/`, say so and I'll fold the README tree in too.

Output: the finished file at `docs/architecture/overview.md`. After writing, verify every `eval/...` path you cited resolves on disk before calling it done.

Out of scope / do not touch: the `eval/` contents themselves (read-only reference), `README.md`'s existing Architecture section content (link to it, don't rewrite it), no changes to `eval/runs/v0.1.0/*.md` or `.err` files.
```

🎯 Target: bare session — plain doc-authoring task, no discovery/autonomy/verification machinery needed
💡 The pointer as given was dead: `docs/architecture/overview.md` doesn't exist and never has (checked `git log --all`), and this repo's real architecture write-up lives inline in `README.md:254`. The prompt corrects "update" → "create" and adds a fence so the new doc doesn't fork from the README section instead of complementing it.
