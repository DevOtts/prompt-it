```
/fable-it

Add scripts/check-version.sh to this repo (prompt-it) that fails when the 0.x.y
version string disagrees across the plugin's four canonical sources.

/goal Guarantee the four version pointers can never silently drift by giving CI/local
runs a script that exits non-zero the instant one of them disagrees, then prove it
actually catches a real mismatch before calling it done.

Context (validated this session, repo is currently at 0.1.0 everywhere):
- @.claude-plugin/marketplace.json — version at `.plugins[0].version` (currently "0.1.0")
- @plugins/prompt-it/.claude-plugin/plugin.json — version at top-level `.version` (currently "0.1.0")
- @SKILL.md (repo root) — version in YAML frontmatter, `version: 0.1.0` (line 17)
- @CHANGELOG.md — version in the most recent `## X.Y.Z — <date>` heading (line 6: `## 0.1.0 — 2026-07-23`)
- Note: `plugins/prompt-it/skills/prompt-it/SKILL.md` is a byte-identical synced copy of the
  root SKILL.md (per commit 03f57e1). Only check the root copy — don't add the nested one
  as a fifth source.

DoD sketch:
1. `scripts/check-version.sh` exists, is executable, extracts the version from all four
   pointers above, and exits 0 when they match — verify: `./scripts/check-version.sh; echo $?`
   on the current repo state returns 0 (baseline is all four = 0.1.0).
2. When exactly one pointer disagrees, the script exits non-zero and its output names
   which file(s) disagree and what the mismatched values are — verify: temporarily bump
   one file's version (e.g. plugin.json → "0.1.1"), rerun the script, confirm non-zero
   exit + a message identifying the mismatch, then revert the edit (`git diff` must show
   the repo back to matching versions before finishing).
3. Proof-of-catch is evidenced, not asserted: capture the actual terminal output of both
   the passing run (step 1) and the failing run (step 2) in the session.

Out of scope / do not touch: don't permanently bump any real version number as part of
this task; don't touch the nested duplicate SKILL.md; don't wire the script into a
pre-commit hook or CI workflow unless asked separately.
```

🎯 Target: fable-it — small task, but user is heading out ("run it to done" unattended) which matches fable-it's autonomous-run trigger over in-session `iterate`.
💡 Validated all four version pointers directly (repo currently at 0.1.0 across the board) and flagged that a second, byte-identical `SKILL.md` copy exists at `plugins/prompt-it/skills/prompt-it/` — scoped the check to the root copy only so the script doesn't get a spurious 5th source.
