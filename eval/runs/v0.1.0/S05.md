Confirmed `data/customers.csv` isn't in this repo — you're referencing your separate scratch repo, so I'm taking that path as given. This ask is small and clear enough for a minimal tighten rather than the full pipeline; routing to **bare** since you said no plugins are installed. I filled the two real ambiguities (which duplicate to keep, where output lands) as flagged assumptions rather than stopping to ask, since they're low-stakes and reversible.

```
Write a script that deduplicates data/customers.csv by email address, case-insensitively (e.g. "Foo@x.com" and "foo@x.com" count as the same row), and reports how many rows were removed.

Assumptions (flag if wrong, otherwise proceed):
- Dedup key: the "email" column, normalized to lowercase for comparison only (don't alter casing in the output).
- Keep the FIRST occurrence of each normalized email (by original row order); drop later duplicates.
- Write the deduped result to data/customers.deduped.csv — don't overwrite the original.
- Preserve the original header and column order.

Do:
1. Read data/customers.csv, confirm it has an "email" column (if the column is missing or named differently, stop and report instead of guessing).
2. Dedupe per the rules above.
3. Write data/customers.deduped.csv.
4. Print a summary: original row count, deduped row count, rows removed.

Out of scope: don't touch other files under data/, don't add config/CLI flags — this is a one-off script.

Run it and show the printed summary as proof it works.
```

🎯 Target: bare session — no plugins installed, so this is a plain script-writing ask with no `*-it` harness to route into.
💡 Two silent ambiguities in your ask (which duplicate row wins, overwrite-vs-new-file) are now explicit assumptions the script will follow — without them the agent could just as easily overwrite your original file or keep the *last* duplicate instead of the first.
