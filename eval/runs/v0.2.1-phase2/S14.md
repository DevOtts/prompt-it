```
/plan-it

Beacon repo: /Users/macbook/Workspace/ANM/Beacon/beacon

GROUNDING: This ask largely re-covers ground that was already planned, built, and closed. Epic #69
("SOW Feedback UX Redesign", archived at docs/implementation/0-done/3-sow-feedback-ux/) shipped
thumbs feedback + a "what went wrong" form on the SOW surface — and its frozen PRD (D1, locked by
Fernando 2026-07-16) explicitly REMOVED a popup-on-complete review modal after Matthew Good flagged
it as chicken-and-egg UX (it pops before you can read what it's asking you to rate). The PRD's own
audit (D8) also found DesignChat and Intel already had inline thumbs widgets and needed no popup.

/goal: Reconcile the new ask against that shipped decision, then close any real gaps in feedback-thumbs
+ thumbs-down reasoning across every LLM chat surface (Intel, Design, SOW regenerations) — deciding
explicitly whether "review modal after each generation" overrides D1 or was meant more narrowly.

CONTEXT PACKAGE:
1. docs/implementation/0-done/3-sow-feedback-ux/PRD-sow-feedback-ux.md — D1–D8 decisions + why the popup was killed
2. docs/implementation/0-done/3-sow-feedback-ux/epics-sow-feedback-ux.md — the 20-case Test Contract already built for SOW
3. frontend/src/components/FeedbackWidget.tsx — thumbs component; confirmed mounted in Intel.tsx:476 and DesignChat.tsx:1441/2095 (trace_id-keyed)
4. frontend/src/components/RegenerateFeedbackModal.tsx + FeedbackReasonForm.tsx — existing pre-flagged thumbs-down intercept, SOW-only today
5. .taskstate/feedback-obs-decisions.md — DEC-1..9 shared contract (rating encoding, trace_id keying) any new mount must respect
6. .claude/pm/tracker.md Epic #59 (E3/E4 — design-chat trace/widget coverage, review-modal + thumbs-down form) — listed Backlog/Ready in this cache; reconcile overlap before treating this as new scope

DoD SKETCH:
1. Every chat surface (Intel, DesignChat, SOW incl. all regenerate/refine actions) has thumbs wired to a real trace_id — verify against each page's widget + `llm_feedback` rows via POST /api/feedback.
2. An explicit, recorded decision exists on the review-modal-after-generation ask vs. D1 (override, or scoped differently) — verify against an amendment to the frozen PRD or a new decision doc.
3. A thumbs-down "what went wrong" form is reachable from every down-vote path, not just SOW's regenerate intercept — verify FeedbackReasonForm mounts per surface.

SCOPE FENCES — Out of scope / do not touch:
- Do not resurrect SOW's popup-on-complete without an explicit new decision superseding D1.
- Do not fork the `1=up/0=down` rating encoding or trace_id keying (DEC-1/DEC-9 frozen).
- Do not touch Epic #59's span/Langfuse plumbing unless this scope genuinely requires it.
```

🎯 Target: plan-it — multi-surface feature cluster (Intel, Design, SOW regenerations) with unresolved scope, not a build-ready ask.
💡 Biggest find: thumbs (FeedbackWidget) already ship on Intel and DesignChat, and a review-modal-after-generation pattern was explicitly built *then killed* on SOW (D1, 2026-07-16, chicken-and-egg UX complaint) — plan-it needs to reconcile that history before anything new gets built, or this reintroduces a UX bug the team already fixed.

Optional refinements (answer if you want the prompt tightened further):
1. Do you want to override D1 and bring a completion-modal back everywhere, or did you mean the passive SowFeedbackCard-style pattern (no popup) extended to Intel/Design instead?
2. "All the regenerations" — Intel has no regenerate action today; do you mean just DesignChat's "Regenerate last answer" + SOW's three buttons?
3. Should this run as an amendment/reopen of Epic #69, or alongside Epic #59 E3/E4 (still Backlog per tracker.md)?
