```
/iterate

Sessions in the Engine workspace are expiring far earlier than intended, which is disrupting authenticated users. (assumed: "Engine workspace" is a separate repo/project outside this session's accessible directory — target session must locate it and confirm the path — flag if wrong)

/goal Fix the auth bug causing sessions to terminate prematurely in the Engine workspace, so sessions last their intended configured duration.

Context:
- Engine workspace repo root (unvalidated — target session must confirm this path first)
- (assumed: likely root-cause areas — session/token TTL config, refresh-token logic, or cookie `maxAge` settings — narrow down via the actual auth/session module once located — flag if wrong)

DoD sketch:
1. Root cause of the premature expiry identified (e.g. misconfigured TTL, broken refresh logic, clock skew) — verification target: a reproduction (log or test) that showed the early expiry before the fix.
2. Fix applied — verification target: a live login that persists for the intended session duration (or an automated session-expiry test passing).
3. No regression to legitimate session termination — verification target: existing auth test suite (or manual check) confirming logout and correctly-timed expiry still work.

Out of scope / do not touch: unrelated auth flows (SSO, password reset, MFA) unless directly implicated in the root cause; do not change the intended session-duration policy itself, only the bug causing it to be shorter than configured.
```

🎯 Target: iterate — single, well-scoped bug (one symptom, one fix-test-verify loop), not a feature cluster or multi-surface change.
💡 Couldn't validate the "Engine workspace" path or the actual auth code — this session's sandbox is scoped to `prompt-it` only, so both are carried as flagged assumptions instead of verified pointers.

Run this from the Engine workspace's repo root, not from `prompt-it`.
