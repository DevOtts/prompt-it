Confirmed fable-it as the route and confirmed the "use lower models / Claude teams" instruction must be dropped — that's explicitly the thing fable-it Step 3 owns. Given this working session is sandboxed to the prompt-it repo, I can't reach the brain-api-core tree to validate the file paths, so they'll carry an unvalidated flag.

```
/fable-it

The ontology client in brain-api-core currently sources its LLM key incorrectly — it needs to draw the per-Space backbone key first, falling back to an env var, the same precedence trigger-draft.service.ts already implements correctly.

/goal: Ship the LLM key-source fix so brain-api-core's ontology client resolves keys with the same per-Space-backbone-then-env-fallback precedence as trigger-draft.service.ts, and the fix is verified working end-to-end.

CONTEXT PACKAGE:
- Pattern to imitate: `trigger-draft.service.ts` — its key-resolution logic is the reference implementation; mirror its precedence order and fallback condition exactly (unvalidated — target session must locate this file first, path not confirmed from this session).
- Target: brain-api-core's ontology client (module/file unconfirmed — target session must locate it) (assumed: a single client file owns LLM key resolution for ontology calls — flag if it's spread across multiple files).
- "Backbone key" = the per-Space key already used elsewhere in the codebase for the LLM backbone (assumed: this term is already defined/named consistently in the codebase — target session should grep for the exact field/config name rather than re-deriving it).

DoD SKETCH:
1. Ontology client requests resolve the LLM key from the current Space's backbone key when one is configured (verify: a Space with a backbone key set produces a call using that key, not the env default).
2. When a Space has no backbone key configured, the client falls back to the env var (verify: a Space without a backbone key produces a call using the env-configured key).
3. The resolution precedence and fallback condition match trigger-draft.service.ts's logic exactly, not just in outcome (verify: side-by-side logic comparison between the two call sites).
4. Existing ontology client callers/tests are unaffected by the change (verify: existing test suite for the ontology client passes).

Out of scope / do not touch: trigger-draft.service.ts itself (read-only reference, not to be modified); other LLM client call sites beyond the ontology client, unless the same key-source bug is confirmed there too.
```

🎯 Target: fable-it — deliverable goal ("ship to done") with a derivable DoD; this is a full fix-verify delivery, not a single in-session loop.
💡 Dropped your "use Claude teams / lower models for subagents" instruction — that's tiering/economics content fable-it's own Step 3 owns exclusively (never duplicated into the prompt per the boundary contract); fable-it will make that split itself. Also: this session is sandboxed to the prompt-it repo, so I couldn't validate the brain-api-core file paths — both are flagged `unvalidated`, and fable-it's own Step 2 grounding should locate them before editing.

Run from the brain-api-core repo root.
