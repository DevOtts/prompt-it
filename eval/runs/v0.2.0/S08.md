```
Trace-naming work, continuing: chat-origin calls already arrive in Langfuse (project praxya) correctly named "chat" — verified, don't touch that path.

/goal Extend the origin-naming mechanism so every LLM call site — not just chat — arrives in Langfuse tagged with its true origin, closing the two confirmed gaps below.

Context:
- Reference pattern: whichever wrapper/client sets `name: "chat"` on the chat origin's Langfuse trace — read and generalize it, don't reinvent the tagging mechanism. (unvalidated — brain-apps/pulse and the praxya Langfuse project sit outside this session's working tree; confirm the actual paths first)
- Gap 1: 3 call sites in brain-apps/pulse emit bare `llm.generate` instead of an origin name.
- Gap 2: automation-triggered calls also arrive unnamed — first confirm whether these share brain-apps/pulse's call wrapper or route through a separate trigger path, since that determines whether one fix covers both gaps or two.

Both FAILs are the same class of defect: the origin-naming convention was wired into the chat entry point only, and nothing enforces it at any other call origin. Treat this as closing that enforcement gap generally, not as 3+N one-off edits.

DoD sketch:
1. Each of the 3 brain-apps/pulse call sites produces a named (non-`llm.generate`) trace in Langfuse project praxya — verify by triggering each site and reading the trace name off the dashboard.
2. Automation-triggered calls produce a named trace in the same project — verify the same way, whichever path they route through.
3. The chat origin still shows as "chat" post-change (no regression).

Out of scope / do not touch: the chat call path (already verified, working); call origins beyond brain-apps/pulse and automation triggers (not confirmed gaps this round — don't expand the hunt to other origins in this loop).
```

🎯 Target: iterate — bounded fix-verify loop over a known, small set of call sites with a concrete Langfuse verification target; no discovery-heavy planning needed.

💡 brain-apps/pulse and the praxya Langfuse project live outside this sandboxed session, so those pointers are kept but flagged unvalidated rather than dropped; also reframed the two FAILs as one mechanism-level gap (class-over-instance) instead of two separate fixes.

Setup: run this from the repo containing brain-apps/pulse (not prompt-it).
