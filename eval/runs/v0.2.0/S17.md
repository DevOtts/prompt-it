```
Organize my ~/Downloads folder by moving files into type-based subfolders. Do not delete anything — every file must still exist afterward, just relocated.

/goal: Sort the top-level files in ~/Downloads into four subfolders — images/, pdfs/, archives/, installers/ — leaving anything that doesn't match untouched at the top level.

Context:
- Target folder: ~/Downloads (create the four subfolders inside it if they don't exist)
- Only sort files sitting directly in ~/Downloads — do not recurse into existing subfolders, and do not re-sort files already inside images/, pdfs/, archives/, or installers/ once created

Type mapping (DoD):
1. images/ — jpg, jpeg, png, gif, heic, webp, svg, bmp, tiff
2. pdfs/ — pdf
3. archives/ — zip, tar, gz, tgz, rar, 7z
4. installers/ — dmg, pkg, exe, msi
5. Anything not matching the above stays exactly where it is — do not invent a catch-all folder
6. Zero deletions — verify the total file count under ~/Downloads (recursively) is identical before and after the move

Out of scope / do not touch:
- No deleting, renaming (beyond the move itself), or editing file contents
- Skip dotfiles/hidden files and in-progress downloads (.crdownload, .part, .download)
- Skip anything currently open or locked
- Don't touch folders that already exist in ~/Downloads other than the four you create

Ambiguity handling: if a file's type is unclear (unknown/unmapped extension, or a filename collision in the destination folder), leave it in place and list it at the end rather than guessing — don't overwrite an existing file of the same name in a destination folder.

When done, print a summary: how many files moved into each of the four folders, and a list of anything skipped or flagged as ambiguous, so I can eyeball the result before I go check the folder myself.
```

🎯 Target: bare Claude Code session (no plugins) — explicitly requested, so this carries slots a routed *-it skill would normally own (uncertainty clause, output contract, stop/scope fences) since nothing downstream provides them.
💡 Added the collision/ambiguity handling and the in-progress-download exclusion (.crdownload/.part) — neither was in your original ask but both are the failure modes that turn "move, don't delete" into an accidental data-loss or half-downloaded-file bug.
