```
/fable-it

Observability epic — continuation from review findings.

Confirmed working (langfuse UI, project `praxya`): chat traces are arriving correctly — global Langfuse deployment and per-tenant projects are live. Don't touch this path; use it as the reference implementation for the two gaps below.

/goal: Every page with an LLM interaction gets user feedback capture, and multi-step LLM pipelines get per-step trace visibility in Langfuse.

Gaps to close (from review-it findings):

1. **Feedback UI is chat-only.** The feedback thumbs component that already works on the chat page is not mounted on the Intel or Design pages, even though both have LLM interactions. Class fix, not a chat-page patch: generalize/extract the existing chat feedback component so it can mount wherever LLM output renders, then add it to Intel and Design. (assumed: the component is a self-contained UI unit already decoupled from chat-specific state — confirm before assuming it can drop into Intel/Design as-is; flag if wrong)

2. **Multi-step intel generation traces as one flat `llm.generate` span.** Cost is invisible per-step. Class fix: instrument each step of the intel generation pipeline as its own nested span/generation under the parent trace (Langfuse supports nested observations), rather than a single wrapping call. (assumed: intel generation is a single backend service/function you can find via a search for `llm.generate` or the Langfuse SDK init — confirm the actual file before implementing; flag if wrong)

Verification targets:
- Feedback thumbs render and submit on the Intel page and the Design page (not just chat) — same UI proof point used for chat.
- The intel generation trace in Langfuse UI (project `praxya`) shows separated child spans/generations per step, not one flat trace.
- Re-check the chat page and existing Langfuse global/per-tenant setup still pass — this fix must not regress the confirmed-working path.

Out of scope / do not touch: Langfuse deployment topology, per-tenant project provisioning, and the chat page's existing trace/feedback wiring — all already verified working.
```

🎯 Target: fable-it — two distinct, testable fixes with a stated DoD and an existing reference implementation (chat) to generalize from; fits an autonomous fix-verify run rather than a single in-session loop.
💡 This session's working directory (`prompt-it`) is unrelated to the observability epic (it's the prompt-it skill's own eval repo) — I couldn't validate any file:line pointers for the feedback component or the intel generation pipeline, so both are flagged inline as assumptions for the target session to confirm first.
