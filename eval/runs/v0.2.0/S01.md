```
plan-it

MCP servers are the standard third-party plug-in surface for tools like HubSpot and Airtable, but the brain system currently has no plug-and-play way for tenants to install them — everything today is bespoke/hardcoded per integration.

/goal: Spec how tenants self-install and configure third-party MCP servers (e.g. HubSpot, Airtable) through the platform, with brain-agent and brain-apps gaining runtime visibility into which MCP servers are installed per tenant.

Context:
- New UI surface: an "MCP" section in tenant integrations settings, following the existing pattern used for the backfill and webhooks sections (unvalidated — this session ran from the `prompt-it` skill repo, which has no product code; the target session must locate the actual product/monorepo and confirm these sections' file paths before planning UI parity).
- Consumers needing visibility: brain-agent and brain-apps (assumed: these are existing subsystems in the product repo, not this repo — confirm their location and current integration-discovery mechanism, if any, first).
- Third-party MCP servers referenced as examples: HubSpot, Airtable (arbitrary/config-driven per tenant, not hardcoded to these two).

Out of scope / do not touch: building or forking the third-party MCP servers themselves (HubSpot's/Airtable's side); redesigning the backfill or webhooks sections beyond using them as a UI/pattern reference; non-MCP integration types.

Light intent signal only (plan-it locks the real DoD):
1. Tenant admin can add, configure, and remove an MCP server entry from integrations settings.
2. brain-agent can enumerate a given tenant's installed MCP servers and reach their tools at runtime.
3. brain-apps can enumerate a given tenant's installed MCP servers for display/use.
```

🎯 Target: plan-it — multi-surface feature (new settings UI + two consuming subsystems) with unresolved scope, even though the pattern-to-imitate (backfill/webhooks) is well specified.
💡 This session runs from the `prompt-it` skill repo, which has none of your product code (no brain-agent, brain-apps, or integrations UI here) — I flagged every product-specific pointer as unvalidated rather than fabricate paths; plan-it's own Phase 0 pre-grounding will resolve them once run from the correct repo.

Run this from the actual product/monorepo containing brain-agent, brain-apps, and the integrations settings UI — not from `prompt-it`.
