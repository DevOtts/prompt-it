```
/iterate

Sessions in the auth system are expiring far sooner than intended in the Engine workspace (assumed: "Engine workspace" = the Engine repo/project directory — flag if wrong, since it's outside this session's accessible tree and unvalidated here).

/goal Diagnose and fix the root cause of premature auth session expiration in the Engine workspace so sessions persist for their intended lifetime, then verify the fix.

CONTEXT PACKAGE
- Engine workspace root/repo path (unvalidated — target session must confirm this path first; not reachable from this sandbox)
- Auth/session module (e.g. session middleware, token/cookie config, refresh logic) — likely location of the root cause (unvalidated — confirm exact file in-session)
- Session lifetime/TTL config (env var, constant, or DB-backed setting) — check for a mismatch between configured value and actual observed behavior

DoD SKETCH
1. A newly created session survives past the previously-observed early-death point — verified by logging in and confirming the session/auth endpoint still returns valid past the old failure window.
2. Actual session lifetime matches the configured/intended expiry (token TTL, cookie maxAge, or refresh-token logic) — verified by inspecting issued token/cookie claims against the configured value.
3. No regression to intentional session termination (logout, explicit expiry, forced invalidation) — verified via existing auth tests or manual logout flow.

Out of scope / do not touch: unrelated auth features (OAuth provider config, roles/permissions), session storage backend/infra changes unless directly implicated in the root cause, any non-auth modules.
```

🎯 Target: iterate — single diagnose→fix→test→verify loop, not a multi-surface feature so plan-it would overreach.
💡 "Engine workspace" couldn't be resolved from this session (sandboxed to the prompt-it repo) — kept as a flagged assumption + unvalidated cross-tree pointer rather than guessing a path; the target session should confirm it first.

Run from the Engine workspace root, not from this prompt-it repo.

Want me to tighten anything once you confirm the actual Engine path or auth module location?
