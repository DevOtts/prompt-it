Skipping that — not load-bearing for the prompt. All four version pointers are validated and currently aligned at 0.2.0.

```
/fable-it

Add a version-consistency check to the prompt-it repo.

/goal: create scripts/check-version.sh that exits non-zero the moment the 0.x.y version string diverges across the repo's four version-bearing files, and prove it catches a real mismatch.

Context package:
- @.claude-plugin/marketplace.json — version source: `.plugins[0].version` (currently 0.2.0)
- @plugins/prompt-it/.claude-plugin/plugin.json — version source: `.version` (currently 0.2.0)
- @SKILL.md:17 — version source: frontmatter `version:` field (currently 0.2.0)
- @CHANGELOG.md:6 — version source: latest `## X.Y.Z` heading (currently 0.2.0)
- `scripts/` does not exist yet in this repo — create it

DoD sketch:
1. `scripts/check-version.sh` exists and is executable; run from repo root it parses the version out of all four files above.
2. Run against the current repo state (all four aligned at 0.2.0): script exits 0.
3. With one file's version deliberately diverged from the other three: script exits non-zero and its output names which file(s) disagree and what version each one shows.
4. After the proof, the repo's four files are back at their original matching values — the check script is the only new artifact.

Out of scope / do not touch: don't wire this into a git hook or CI workflow — the script itself is the deliverable; don't change the actual shipped version number in any of the four files; don't touch unrelated repo files (eval/, README.md, prompt-examples.txt, etc.).
```

🎯 Target: fable-it — deliverable has a stated, trivially-verifiable DoD and "I'm heading out" + "run it to done" signals an unattended autonomous run, matching fable-it's exact trigger language.
💡 Validated all four version pointers on disk (they're currently consistent at 0.2.0, which is why DoD item 3 requires the proof to introduce *and then revert* a deliberate mismatch rather than leaving the repo in a broken state).
