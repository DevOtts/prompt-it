```
/iterate

GROUNDING: The praxya tenant chat agent's system prompt conflates "cliente" with the org entity type=client. When the owner asks "quantos clientes recorrentes tivemos este mês?", the agent searches org records (type=client), finds none matching "recorrentes," and answers "no clients found" — but "clientes recorrentes" here means Shopify repeat buyers, an unrelated data source.

/goal: Fix the tenant chat agent's system prompt so "cliente"/"clientes recorrentes" queries resolve to Shopify repeat-buyer data, not org type=client records, eliminating the false "no clients found" response.

CONTEXT PACKAGE:
- Repro query (PT-BR): "quantos clientes recorrentes tivemos este mês?" — tenant: praxya
- Current (wrong) behavior: agent treats "clientes" as org type=client, finds 0 matches, replies "no clients found"
- Correct behavior: "clientes recorrentes" = Shopify repeat/returning buyers this month
- (assumed: the system prompt and Shopify repeat-buyer query/tool both already exist in the target repo you're running this in — flag if the Shopify-side lookup still needs to be built, since that changes scope)

DoD SKETCH:
1. Given the exact repro query, the agent's response reflects a Shopify repeat-buyer count/answer, not "no clients found"
2. System prompt contains an explicit terminology rule: "cliente"/"clientes recorrentes" in owner-facing queries → Shopify repeat buyers; org type=client is a distinct, separately-named concept the agent doesn't conflate with it
3. A control query that legitimately means org type=client (e.g. "quantos clientes [orgs] temos cadastrados?") still resolves to org records — no regression from the fix

SCOPE FENCES: Out of scope — do not modify the Shopify repeat-buyer query/service logic itself (only how the agent's prompt routes to it), do not touch org type=client schema or queries, do not relabel other ambiguous terms not implicated in this bug.
```

🎯 Target: iterate — single fix→test→verify loop scoped to one system-prompt edit with a concrete repro query.
💡 This repo (prompt-it) has no tenant-chat-agent codebase — "praxya"/this bug only exists as eval fixture `eval/sample-prompts.md:39`. I couldn't validate the actual system-prompt file path, so run this from the real product repo; the DoD's first step should be locating that file and the existing type=client query.

Run from the repo that actually contains the praxya tenant chat agent (not this prompt-it repo).

Which repo is that — do you want me to look for it locally, or will you cd there yourself before pasting this in?
