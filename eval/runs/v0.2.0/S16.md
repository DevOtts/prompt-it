```
/iterate

The tasks-page dropdown throws "ResizeObserver loop limit exceeded" and the element detaches mid-click during our CDP e2e run, so that spec is red.

/goal Get the CDP e2e suite passing against the real page — fix the underlying interaction bug, don't skip, weaken, or retry-pad the test to mask it.

Context:
- Dropdown component backing the tasks-page filter/actions menu (assumed: not in this working tree — flag if wrong; locate via grep for "ResizeObserver" or the console-error handler in the frontend app repo).
- The failing CDP e2e spec that drives this dropdown's click/select step (assumed: same repo as above — flag if wrong).
- Symptom pairing (ResizeObserver loop-limit warning + mid-click detach) reads as the dropdown re-rendering/repositioning while CDP's click is still resolving — a real layout/interaction race, not flaky infra.

DoD sketch:
1. The CDP e2e spec for the tasks-page dropdown passes, unmodified in scope (not skipped, not xfail'd, not deleted) — target: the e2e suite run output for that spec.
2. No "ResizeObserver loop limit exceeded" warning/error fires during the dropdown open+click flow — target: browser console output captured during that spec's run.
3. The fix addresses the layout/interaction race itself, not the symptom — target: the click still exercises the real dropdown element with no added sleep/retry padding or global ResizeObserver suppression in the diff.

Out of scope / do not touch: do not skip, weaken, or delete the failing test; do not suppress the ResizeObserver error via a global handler as the fix; do not paper over the race with sleeps/retries/longer waits; leave other e2e specs untouched.
```

🎯 Target: iterate — single diagnose→fix→verify loop on one failing e2e spec, not a multi-surface planning task.
💡 This working tree (prompt-it) has no app code matching "tasks-page"/dropdown — it's actually where this exact prompt lives as eval sample S16. Since the real target repo isn't this one, the dropdown/e2e file pointers are flagged `(assumed — flag if wrong)` rather than validated; point /iterate at the actual frontend repo when you run this.
