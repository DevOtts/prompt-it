```
/iterate

Fix the three hardcoded-locale defects in brain-admin-ui so tenant.language actually drives rendering — via ONE shared, reusable formatting/localization mechanism, not per-instance ternaries. A prior fix patched these same three spots with inline ternaries on `tenant.language` and was reverted for being unmaintainable; the mechanism, not just the values, needs to change this time.

Context:
- Grep anchor `"Painel"` — locates the hardcoded header title (assumed: repo not in this working tree, confirm exact file/path first)
- Grep anchor for the date-format literal `DD/MM/YYYY` — locates the tasks-table date formatting (assumed: confirm exact file/path first)
- Grep anchor `"R$"` — locates the billing-widget currency symbol (assumed: confirm exact file/path first)
- `tenant.language` is the correct locale signal (confirmed by the earlier, reverted attempt) — reuse it, don't rediscover the source
- Failed approach, do not repeat: inline ternaries (`tenant.language === 'pt' ? X : Y`) directly in each component — reverted for unmaintainability

Definition of Done:
1. Admin header renders a localized title (not the literal "Painel") for an English-language tenant — verify: dashboard header, en-locale tenant session
2. Tasks table date column renders per `tenant.language` (not hardcoded DD/MM/YYYY) — verify: tasks table, both an en-locale and a pt-locale tenant session
3. Billing widget currency symbol matches tenant locale (not hardcoded "R$") — verify: billing widget, non-BRL tenant session
4. Fix lands as one shared/reusable localization mechanism (formatter util, messages file, or existing i18n lib if the codebase already has one) — verify: grep confirms no new inline `tenant.language ? … : …` ternaries in the three touched files

Out of scope / do not touch: any page or component not named in these three findings — this is not an app-wide i18n migration; don't touch tenant.language's own resolution/source logic, only how these three components consume it.
```

🎯 Target: iterate — single fix-test-verify loop, in-session; all three FAILs share one root cause and one prior failed fix, which is exactly iterate's shape
💡 Reframed the three FAILs as one class-level defect (per-instance hardcoding) rather than three point fixes, and made the reverted ternary approach an explicit "don't repeat" so the next session doesn't reintroduce it — brain-admin-ui isn't in this working tree, so all three grep anchors are flagged unvalidated for the target session to confirm first.
