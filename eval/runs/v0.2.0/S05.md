```
Write a script that deduplicates data/customers.csv, treating two rows as duplicates when their email addresses match case-insensitively (e.g. "Foo@Bar.com" and "foo@bar.com" are the same customer). Keep the first occurrence of each email (assumed — flag if you'd rather keep the last or the most-complete row per duplicate group) and write the result to a new file, e.g. data/customers.deduped.csv (assumed — flag if you want an in-place overwrite instead). Print a report to stdout with the original row count, the deduped row count, and how many rows were removed. Use whatever scripting language is already idiomatic for this repo (assumed: Python — flag if you prefer bash/awk or Node). (assumed: data/customers.csv exists at that path in your scratch repo — this session couldn't validate it, since it's a different repo than the one prompt-it is running in.)
```

🎯 Target: bare session — plain script task, no plugins installed and no harness needed for a single dedupe-and-report script.
💡 The `data/customers.csv` pointer doesn't exist in prompt-it's own working tree (checked — no `data/` dir here), so it's carried as an unvalidated cross-tree assumption; also flagged the two real ambiguities (which duplicate row to keep, overwrite vs. new output file) since your ask didn't specify them.
