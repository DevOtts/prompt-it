```
Create docs/architecture/overview.md documenting the prompt-it eval suite under eval/.
(assumed: docs/architecture/overview.md does not exist yet anywhere in the repo — the only current architecture doc is README.md's "## Architecture / What's inside" section, whose tree diagram predates eval/ entirely. Assuming this means create the file fresh under a new docs/architecture/ directory; flag if you actually meant updating that README section instead.)

/goal: document what the eval suite is, how its pieces fit together, and how to run it — so the eval suite is discoverable the same way the rest of the repo layout is.

Context package:
- eval/sample-prompts.md — the 20 rough input samples + coverage matrix (routes, modes, dead-pointer/ambiguity cases covered)
- eval/expected-prompts.md — the paired oracle: per-sample exemplar + binding MUST/MUST-NOT checklist that review-it judges against
- eval/scripts/run-eval.sh — runs one sample through a fresh headless `claude -p` session against the installed plugin
- eval/runs/v0.1.0/ and eval/runs/v0.2.0/ — versioned per-sample run outputs (.md + .err), one folder per eval cycle
- README.md "## Architecture / What's inside" (line 254) — existing repo-layout doc; keep this new file's account of eval/ consistent with it, don't duplicate the whole tree

DoD sketch:
1. Doc explains the sample↔oracle pairing (same ID across the two files) and the pass rule (every MUST holds, no MUST-NOT violated) — verify against eval/expected-prompts.md's actual global checks (G1-G6).
2. Doc explains how to invoke a run (run-eval.sh usage, EVAL_MODEL override) and where output lands (eval/runs/<version>/<SID>.md) — verify against eval/scripts/run-eval.sh's actual invocation.
3. Doc's account of eval/ doesn't contradict README.md's Architecture section — verify by re-reading both side by side.

Out of scope / do not touch: SKILL.md, references/targets.md, the eval fixtures/samples/oracle content itself. Documentation only — no changes to eval behavior.

If the docs/architecture/ location or scope is wrong, say so rather than guessing further and stop for direction.
```

🎯 Target: bare session — plain single-file doc task in this repo, no harness needed for a scoped documentation write.
💡 The named target path doesn't exist anywhere in the repo (verified via `find`) — flagged as an assumption to create it fresh rather than silently redirecting you to README.md's existing Architecture section, which is the closest real analog.
