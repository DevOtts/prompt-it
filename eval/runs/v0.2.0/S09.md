```
Continue the Airtable write feature — review-it found two FAILs, not a pass. Read this as the ground truth; do not re-trust the prior "green" CDP run.

/goal: Make the Airtable tasks write path actually work end-to-end — the tasks dropdown must run error-free for a real human, and a saved task must be independently confirmed to land in the Airtable base/table.

Review findings (verbatim from the review, not re-derived):
1. The CDP e2e suite reported green, but a human driving the tasks dropdown hits real UI errors. The suite is not catching what a human catches — treat this as an automated-coverage gap in the harness/dropdown interaction, not a flaky-test issue.
2. Saving to tasks required the integration to be enabled first, and nobody confirmed the record actually reached Airtable — the app-side "success" was never checked against the external system.

Already tried — do not repeat: re-running the same CDP suite with longer waits. Produced the same false green. The fix is not timing; find why the suite doesn't surface the dropdown error a human sees (missing console/DOM-error assertion, wrong selector/state, or a race the suite doesn't wait on).

DoD sketch (verification targets, not methods):
1. A human (or a CDP run that asserts on console/page errors, not just suite exit code) drives the tasks dropdown with zero UI errors.
2. A task saved through the app is confirmed present in the actual Airtable base/table — the record itself is the evidence, not the app's success response.
3. The integration-enablement precondition is either handled by the write path itself or explicitly checked before save, so "not enabled" fails loudly instead of silently no-op-ing.

Out of scope / do not touch: other Airtable integrations or object types beyond tasks; do not "fix" this by adding waits/retries to the existing CDP suite — that path is already ruled out.

(assumed: this is a single fix→verify loop scoped to the tasks dropdown + Airtable write path, not a multi-surface rebuild — flag if the scope is actually larger)
```

🎯 Target: iterate — this is a bounded diagnose→fix→verify loop on one feature (two concrete FAILs), not an open-ended build; fable-it/plan-it would be overkill here.
💡 Rewrote the two FAILs as one mechanism-level gap each (harness fidelity, external-system verification) instead of instance fixes, and hard-coded the "longer waits" dead end as a do-not-repeat so the next session doesn't rediscover it.

Note: no repo/file pointers were available in this conversation for the Airtable feature (this working directory is prompt-it itself) — if you have the actual review-it report, run-memory.md, or repo path, paste them and I'll swap the assumed line for validated citations.
